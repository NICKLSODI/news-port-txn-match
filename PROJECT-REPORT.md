# MatchPort — เอกสารโครงการฉบับละเอียด

> เอกสารนี้เขียนไว้เป็นวัตถุดิบสำหรับสรุปเป็นรายงานฝึกงาน จึงลงรายละเอียดมากกว่าที่จะใช้จริงทั้งหมด
> ตัวเลขทุกตัวดึงจากระบบจริง ณ วันที่ 7 สิงหาคม 2569 ไม่ใช่ตัวเลขสมมติ
> เอกสารเชิงวิศวกรรมสำหรับผู้รับช่วงต่ออยู่ที่ `ENGINEERING-HANDOVER.md` · วิธีติดตั้งอยู่ที่ `SETUP.md`

---

## สารบัญ

1. [ปัญหาที่แก้](#1-ปัญหาที่แก้)
2. [ภาพรวมระบบ](#2-ภาพรวมระบบ)
3. [สถาปัตยกรรม](#3-สถาปัตยกรรม)
4. [ข้อมูลนำเข้า](#4-ข้อมูลนำเข้า)
5. [การแปลงรหัสสินทรัพย์](#5-การแปลงรหัสสินทรัพย์)
6. [การดึงและตีความข่าว](#6-การดึงและตีความข่าว)
7. [อัลกอริทึมจับคู่](#7-อัลกอริทึมจับคู่)
8. [การใช้ AI](#8-การใช้-ai)
9. [ฟีเจอร์ปันผล](#9-ฟีเจอร์ปันผล)
10. [การส่งอีเมล](#10-การส่งอีเมล)
11. [หน้าจอ](#11-หน้าจอ)
12. [หลักการออกแบบที่ยึดตลอด](#12-หลักการออกแบบที่ยึดตลอด)
13. [ตัวเลขผลลัพธ์](#13-ตัวเลขผลลัพธ์)
14. [ปัญหาที่เจอและวิธีแก้](#14-ปัญหาที่เจอและวิธีแก้)
15. [ข้อจำกัดที่ยังเหลือ](#15-ข้อจำกัดที่ยังเหลือ)
16. [สิ่งที่ได้เรียนรู้](#16-สิ่งที่ได้เรียนรู้)

---

## 1. ปัญหาที่แก้

### 1.1 สถานการณ์เดิม

บริษัทหลักทรัพย์มีนักลงทุนสัมพันธ์ (RM — Relationship Manager) ดูแลลูกค้าคนละหลายร้อยราย
ทุกเช้าบริษัทออกบทวิเคราะห์และข่าวหลายสิบชิ้น RM ต้องตอบคำถามเดิมทุกวัน:

> **"ข่าววันนี้เกี่ยวกับลูกค้าคนไหนของฉันบ้าง และควรโทรหาใครก่อน"**

การทำด้วยมือมีปัญหาสามอย่าง:

| ปัญหา | ผลที่ตามมา |
|---|---|
| ต้องเปิดพอร์ตลูกค้าทีละคนมาเทียบกับข่าวทีละชิ้น | ใช้เวลาหลายชั่วโมง ทำได้ไม่ครบ |
| จำได้แค่ลูกค้ารายใหญ่ที่คุ้นเคย | รายกลางถูกมองข้ามทั้งที่รวมกันแล้วมูลค่าสูงกว่า |
| ไม่มีเกณฑ์ว่าควรโทรใครก่อน | เรียงตามความคุ้นเคย ไม่ใช่ตามมูลค่า |

### 1.2 เป้าหมาย

เปลี่ยนงาน "อ่านข่าวแล้วนึกเอาเองว่าเกี่ยวกับใคร" ให้เป็นรายการที่จัดลำดับมาแล้ว
โดยที่ **ทุกบรรทัดต้องอธิบายได้ว่าทำไมถึงอยู่ตรงนั้น** — ไม่ใช่กล่องดำที่บอกแค่ผลลัพธ์

### 1.3 ผู้ใช้สองกลุ่ม

ระบบต้องตอบสองมุมมองพร้อมกัน:

- **RM รายคน** — เห็นเฉพาะบุ๊กตัวเอง ต้องการรายชื่อที่โทรได้เลย
- **หัวหน้าทีม / คนนอกทีม** — เห็นภาพรวมทั้งบริษัท ใช้ตัดสินใจเชิงบริหาร

ทุกหน้าจึงมีตัวกรอง RM และ **ตั้งค่าเริ่มต้นเป็นภาพรวม** เสมอ

---

## 2. ภาพรวมระบบ

### 2.1 ลำดับการทำงาน

```
ไฟล์พอร์ตลูกค้า (Excel)  ─┐
ไฟล์ธุรกรรม (Excel)      ─┤
                          ├─→ แปลงรหัส ─→ ฐานข้อมูล ─┐
Cafe Invest API (ข่าว)   ─┤                          ├─→ จับคู่ ─→ จัดลำดับ ─→ หน้าจอ
INVX Data Book (PDF)     ─┤                          │                        └─→ อีเมล
Yahoo Finance API        ─┘                          │
                                                      │
Claude Code CLI ─→ อ่านข่าวเชิงลึก ────────────────┘
```

### 2.2 ขนาดโครงการ

| ส่วน | ไฟล์ | บรรทัด |
|---|---:|---:|
| Backend (Python) | 19 | 8,811 |
| Scripts (Python) | 11 | 1,788 |
| Frontend (TypeScript/React) | 27 | 8,218 |
| **รวม** | **57** | **18,817** |

**ไฟล์หลักฝั่ง backend**

| ไฟล์ | บรรทัด | หน้าที่ |
|---|---:|---|
| `main.py` | 1,666 | API ทั้งหมด 40 endpoint |
| `news.py` | 1,171 | ดึงข่าว แกะเนื้อหา ระบุหุ้นในข่าว |
| `mailer.py` | 956 | ประกอบรายงาน HTML + ส่งผ่าน Outlook |
| `enrich.py` | 786 | ให้ Claude อ่านข่าวเชิงลึก + ตรวจคำตอบ |
| `tables.py` | 746 | ตารางกฎทั้งหมด (แปลงจากไฟล์ spec) |
| `briefing.py` | 498 | อ่านทิศทางข่าว + ประเด็นที่ควรคุย |
| `upload.py` | 421 | ตรวจไฟล์ก่อนนำเข้า (รวมการกัน PII) |
| `mapping.py` | 418 | แปลงรหัสสินทรัพย์เป็นรหัสกลาง |
| `ingest_customers.py` | 406 | คำนวณ feature และ persona ของลูกค้า |
| `matching.py` | 378 | อัลกอริทึมจับคู่ + ให้คะแนน |
| `db.py` | 303 | โครงฐานข้อมูล |
| `dividends.py` | 290 | ตารางปันผลจาก Data Book |
| `verify.py` | 219 | ตรวจผลลัพธ์ของ AI |
| `yahoo.py` | 151 | ดึง sector หุ้นต่างประเทศ |
| `prices.py` | 122 | กราฟราคาจาก Yahoo |

### 2.3 เทคโนโลยี

**Backend** — Python 3.14 · FastAPI · SQLite · pandas (อ่าน Excel) · pypdf (แกะ PDF) · requests

**Frontend** — React 19 · TypeScript · Vite · TanStack Query · React Router · lightweight-charts

**เหตุผลที่เลือก SQLite ไม่ใช่ฐานข้อมูลเซิร์ฟเวอร์**
ระบบนี้รันบนเครื่องผู้ใช้แบบโปรแกรมเดสก์ท็อป ข้อมูลทั้งหมดอยู่ในไฟล์เดียว
ไม่ต้องติดตั้งเซิร์ฟเวอร์ ไม่ต้องตั้งค่าเครือข่าย ก๊อปไฟล์ไปเครื่องอื่นได้ทันที
ปริมาณข้อมูลจริง (156,442 ธุรกรรม) SQLite รับได้สบาย

---

## 3. สถาปัตยกรรม

### 3.1 ฐานข้อมูล

12 ตาราง · ข้อมูลจริง ณ ปัจจุบัน

| ตาราง | แถว | คอลัมน์ | เก็บอะไร |
|---|---:|---:|---|
| `transactions` | 156,442 | 15 | ธุรกรรมย้อนหลัง 6 เดือน |
| `holdings` | 15,369 | 15 | สิ่งที่ลูกค้าถืออยู่ ณ วันที่ตัดยอด |
| `matches` | 15,331 | 14 | ผลจับคู่ข่าว × ลูกค้า |
| `customers` | 1,106 | 18 | ลูกค้า + feature ที่คำนวณแล้ว |
| `entity_pairs` | 864 | 4 | คู่หุ้นที่ถูกกล่าวถึงด้วยกันบ่อย |
| `unmapped` | 518 | 9 | ของที่ระบบจัดการไม่ได้ |
| `articles` | 355 | 47 | ข่าว + ผลวิเคราะห์ |
| `review_queue` | 144 | 7 | รายการรอคนตรวจ |
| `dividends` | 78 | 15 | ตารางปันผลรายเดือน |
| `settings` | 12 | 2 | ค่าตั้งระบบ |
| `price_cache` | 9 | 5 | แคชกราฟราคา |
| `ingest_log` | 5 | 6 | ประวัติการนำเข้า/ส่งอีเมล |

### 3.2 หลักการแยกชั้นข้อมูลอ้างอิง

จุดที่ใช้เวลาออกแบบมากที่สุด — แยกเป็นสามชั้นที่แก้ด้วยวิธีต่างกัน

| ชั้น | ไฟล์ | ใครแก้ | ทำไมต้องแยก |
|---|---|---|---|
| **กฎในโค้ด** | `tables.py` | ต้องรีวิวโค้ด | เป็นตรรกะที่ต้องมีคนอ่านก่อนเปลี่ยน |
| **สร้างจาก spec** | `refdata/generated.json` | ห้ามแก้มือ | สร้างทับทุกครั้งที่ build |
| **คนอนุมัติ** | `refdata/overrides.json` | ผ่าน `approve.py` เท่านั้น | ของที่คนตัดสินใจแล้ว ทำใหม่ไม่ได้ถ้าหาย |

ถ้าไม่แยก `tables.py` จะโตขึ้นทุกครั้งที่เจอรหัสใหม่ จนกลายเป็นไฟล์ที่ไม่มีใครกล้าแก้

ปัจจุบัน `overrides.json` มี **2,398 รายการ** (sector ของหุ้นต่างประเทศ)

### 3.3 กฎที่อ้างอิงในโค้ด

โปรเจกต์นี้มาจากเอกสาร spec 8 ชุด (STEP1–STEP8) โค้ดอ้างเลขกฎกำกับไว้ **122 จุด**

รูปแบบเลขกฎ:
- `R1.x` – `R6.x` — กฎจาก spec แต่ละ STEP
- `GAP-xx` — ช่องว่างที่ spec ไม่ได้ครอบคลุม แต่ของจริงมี (23 รายการ)
- `AI-01/02` — กฎเรื่องการใช้ AI
- `DIV-01` — กฎเรื่องปันผล

**ทำไมต้องอ้างเลขกฎในโค้ด** — เมื่อผลลัพธ์ออกมาแปลก คนตรวจต้องย้อนได้ว่ามาจากกฎข้อไหน
และเมื่อ spec เปลี่ยน ต้องหาได้ว่าต้องแก้ตรงไหนบ้าง

---

## 4. ข้อมูลนำเข้า

### 4.1 ไฟล์ลูกค้า

รับ Excel สองไฟล์: พอร์ตถือครอง และ ธุรกรรมย้อนหลัง

**หลักการสำคัญ: ไฟล์ล่าสุดคือความจริง (R1.7)**
อัปโหลดชุดใหม่ = ล้างของเดิมทิ้งแล้วคำนวณใหม่ทั้งหมด ไม่มีการต่อยอดจากของเก่า
เพราะข้อมูลพอร์ตเป็นภาพ ณ วันที่ตัดยอด ไม่ใช่รายการเพิ่ม-ลด

### 4.2 การกัน PII

`upload.py` **ปฏิเสธไฟล์ที่มีข้อมูลระบุตัวบุคคล** ก่อนนำเข้า ตรวจ:
- คอลัมน์ที่มีอีเมล
- ชื่อ-นามสกุลจริง
- เลขบัตรประชาชน / เบอร์โทร

ระบบทั้งระบบจึงรู้จักลูกค้าเป็นรหัสอย่าง `CUST00123` เท่านั้น
ตารางเทียบรหัสกับชื่อจริงอยู่นอกระบบ อยู่กับผู้ดูแลเท่านั้น

**ผลต่อการออกแบบ** — รายงานที่ส่งออกไปทางอีเมลจึงต้องเขียนกำกับว่า
"รหัสนี้ไม่ใช่ชื่อจริง ถ้าต้องการตารางเทียบให้ขอจากผู้ดูแลระบบ"
ไม่งั้นคนรับจะคิดว่าไฟล์เสีย

### 4.3 การคำนวณ feature ลูกค้า

จากข้อมูลดิบ ระบบคำนวณต่อเป็นคุณลักษณะที่ใช้จับคู่:

| feature | คำนวณจาก | ใช้ทำอะไร |
|---|---|---|
| `persona` | สัดส่วนสินทรัพย์ | คัดว่าข่าวประเภทไหนควรถึงใคร |
| `portfolio_tier` | มูลค่าพอร์ต | จัดกลุ่มขนาด |
| `trade_frequency` | จำนวนธุรกรรม 6 เดือน | ดูสไตล์การลงทุน |
| `days_since_last_trade` | ธุรกรรมล่าสุด | หาลูกค้าที่เงียบไป |
| `asset_mix` | สัดส่วนแต่ละประเภท | แสดงผล + จับคู่ระดับ L5 |
| `sector_exposure` | หุ้นไทยที่ถือ × sector | จับคู่ระดับ L3 |
| `watchlist` | เคยเทรดใน 90 วันแต่ไม่ถือแล้ว | จับคู่ระดับ L2 |

**การกระจาย persona จริง** (1,106 คน)

| persona | จำนวน | สัดส่วน |
|---|---:|---:|
| US_OFFSHORE | 531 | 48.0% |
| FUND_DIY | 194 | 17.5% |
| THAI_STOCK | 187 | 16.9% |
| DORMANT | 99 | 9.0% |
| CRYPTO | 72 | 6.5% |
| FUND_ROBO | 16 | 1.4% |
| BOND | 4 | 0.4% |
| DERIVATIVES | 3 | 0.3% |

**ข้อสังเกตที่สำคัญต่อการออกแบบ** — เกือบครึ่งเป็นนักลงทุนหุ้นต่างประเทศ
แต่บทวิเคราะห์ของบริษัทเน้นหุ้นไทย ทำให้ฟีเจอร์ที่อิงข้อมูลหุ้นไทยครอบคลุมลูกค้าได้จำกัด
(ดูหัวข้อ 9.3 เรื่องสไตล์พอร์ต)

---

## 5. การแปลงรหัสสินทรัพย์

### 5.1 ปัญหา

รหัสสินทรัพย์ในไฟล์ลูกค้ากับในข่าวเขียนคนละแบบ ต้องแปลงให้เป็นรหัสกลางก่อนจับคู่

ตัวอย่างความยุ่งของจริง:

| ที่มา | รูปแบบ | ตัวอย่าง |
|---|---|---|
| หุ้นไทย | ตัวย่อล้วน | `PTTEP` |
| หุ้นต่างประเทศ | ตัวย่อ + รหัสตลาด | `NVDA:xnas` |
| หุ้นฮ่องกง | ตัวเลขเติมศูนย์ 5 หลัก | `00700:xhkg` |
| DR (ตราสารแสดงสิทธิ) | ชื่อ + เลขผู้ออก | `TENCENT80` → `00700:xhkg` |
| กองทุน | รหัสกองทุน | `SCBNDQ(A)` |
| TFEX | รหัสสัญญา | `S50U26` |

### 5.2 วิธีแก้

`mapping.py` แปลงตามลำดับ: หุ้นไทย → หุ้นนอก → DR → กองทุน → อนุพันธ์ → คริปโต

**หลักที่ยึด: ไม่มั่นใจ ไม่เดา**
รหัสที่แปลงไม่ได้จะถูกบันทึกลงตาราง `unmapped` พร้อมเหตุผล ไม่ใช่ทิ้งเงียบ
ปัจจุบันมี 518 รายการ แยกตามประเภท:

| ประเภท | รูปแบบที่ไม่รู้จัก | พบรวม |
|---|---:|---:|
| `holding_code` | 95 | 1,753 ครั้ง |
| `full_text` | 177 | 406 ครั้ง |
| `news_entity` | 19 | 308 ครั้ง |
| `ai` | 224 | 268 ครั้ง |

**ตัวตรวจสอบสำคัญ (R3.24)** — รหัสที่แปลงแล้วต้องมีคนถือจริงในพอร์ตลูกค้า
ถ้าไม่มี ถือว่าแปลงผิด ไม่รับผลนั้น
ด่านนี้ทำให้ต่อให้ระบบเดาผิด ก็ไม่มีทางส่งข่าวของบริษัทที่ไม่มีใครถือไปให้ลูกค้า

### 5.3 การเติม sector หุ้นต่างประเทศ

**ปัญหา** — บริษัทมี Coverage List ของหุ้นไทย 97 ตัวพร้อม sector
แต่หุ้นต่างประเทศไม่มีแหล่งข้อมูล sector เลย ทำให้จับคู่ระดับ L3 (กลุ่มอุตสาหกรรม) ใช้ไม่ได้

**วิธีแก้** — ดึงจาก Yahoo Finance `quoteSummary` API (module `assetProfile`)
ซึ่งใช้การจัดหมวดตามมาตรฐาน GICS

**อุปสรรคที่เจอ** — Yahoo บังคับ cookie + crumb แล้ว ยิงตรงได้ `Invalid Crumb`
ต้องขอ cookie จาก `fc.yahoo.com` ก่อน แล้วแลก crumb จาก `/v1/test/getcrumb`
(จุดที่พลาดง่าย: `fc.yahoo.com` ตอบ HTTP 404 เสมอ แต่ยังแนบ cookie มาด้วย
ถ้าเขียนโค้ดแบบมองว่า 404 คือ error จะไม่ได้ cookie เลย)

**ผลลัพธ์**

| | จำนวน |
|---|---:|
| หุ้นนอกที่ลูกค้าถือทั้งหมด | 3,181 |
| ดึง sector สำเร็จ | **2,396** (75%) |
| ไม่สำเร็จ | 785 |
| — Yahoo ไม่มีข้อมูล (ETF/กองทุน) | 487 |
| — ตลาดที่ยังไม่มี mapping (เช่นเวียดนาม) | 298 |

การกระจาย sector: Technology 485 · Healthcare 350 · Industrials 346 · Consumer Cyclical 263 ·
Financial Services 215 · Basic Materials 209 · Consumer Defensive 151 · Communication Services 128 ·
Energy 123 · Real Estate 75 · Utilities 51

ทุกรายการต้องผ่าน `scripts/approve.py` ก่อนใช้จริง เพราะชื่อ sector ของ Yahoo
เป็นภาษาอังกฤษคนละชุดกับ Coverage List หุ้นไทย ต้องมีคนดูก่อนว่าไม่ตีกัน

---

## 6. การดึงและตีความข่าว

### 6.1 แหล่งข่าว

Cafe Invest API ของบริษัท (`/api/content/search`) ดึงบทความล่าสุดทั้งเว็บ

**ปัญหาที่เจอกับ API**

| ปัญหา | วิธีแก้ |
|---|---|
| ตอบ 403 ถ้าไม่ส่ง User-Agent | ส่ง User-Agent แบบเบราว์เซอร์ |
| `category` เป็น object `{id,name,slug}` ไม่ใช่ข้อความ | แกะ `slug` ออกมาก่อนใช้ |
| ส่ง summary มาแค่ย่อหน้าแรก (เฉลี่ย 522 ตัวอักษร) | ดึงเนื้อหาเต็มจากหน้าบทความแยก |
| ไม่มี rate limit ฝั่งเซิร์ฟเวอร์ | จำกัดเอง 0.4 วินาทีต่อครั้ง |

**หลักที่ยึด (R2.5)** — ถ้า field ที่จำเป็นหายไปจาก API ให้หยุดและแจ้งเตือน **ห้ามเดา**
เพราะโครงสร้าง API เปลี่ยนแล้วเดาต่อ = ข้อมูลผิดทั้งระบบโดยไม่มีใครรู้

### 6.2 การดึงเนื้อหาเต็ม

เนื้อหาเต็มไม่ได้มากับ API ต้องโหลดหน้าบทความแล้วแกะจาก payload ของ Next.js (RSC stream)

**จุดที่พลาดง่าย** — chunk ของ RSC เขียนเป็น `<id>:T<ความยาว>,<เนื้อหา>`
โดยความยาวนับเป็น **ไบต์ ไม่ใช่ตัวอักษร** ภาษาไทยตัวละ 3 ไบต์
ถ้าตัดตามจำนวนตัวอักษรจะกินไปถึง chunk ถัดไป ได้เนื้อซ้ำและ marker ปนมา

### 6.3 การระบุหุ้นในข่าว

ไล่ตามลำดับความน่าเชื่อถือ:

1. **entity จาก API** — เชื่อถือได้สูงสุด
2. **ตัวย่อในหัวข้อ/เนื้อหา** ที่ตรงกับ Universe ของลูกค้า
3. **ท่อน "หุ้นเด่น" (Top Picks)** ในเนื้อหาเต็ม (GAP-23)
4. **คำค้น sector ภาษาไทย** สำหรับบทวิเคราะห์รายอุตสาหกรรมที่ไม่เอ่ยชื่อหุ้น

**เกรดคุณภาพหลักฐาน** — ทุกบทความได้เกรดว่าหลักฐานหนาแค่ไหน

| เกรด | จำนวน | ความหมาย |
|---|---:|---|
| `confirmed` | 279 | มีตัวย่อชัดเจนในเนื้อหา |
| `auto_verified` | 65 | ระบบตรวจแล้วผ่าน |
| `weak` | 6 | หลักฐานบาง ต้องมีคนดู |

### 6.4 การอ่านทิศทางข่าว

`briefing.py` อ่านทิศทางเป็นสามชั้น แยกน้ำหนักชัดเจน

| ชั้น | ที่มา | น้ำหนัก |
|---|---|---|
| 1 | คำแนะนำที่ INVX ประกาศเอง (Outperform/Neutral) | สูงสุด |
| 2 | ผลประกอบการเทียบกับที่คาด | ข้อเท็จจริง |
| 3 | โทนของพาดหัว | ต่ำสุด — ติดป้ายว่าเป็นการอ่านถ้อยคำ |

**สิ่งที่จงใจไม่ทำ** — ไม่ยุบเป็นคำเดียวว่า "ข่าวดี/ข่าวร้าย"
ถ้าบทความพูดสองทาง ระบบแสดงว่าสองทาง พร้อมบอกว่าแต่ละทางกระทบใคร

### 6.5 การแยกส่วน "มุมมองของ InnovestX"

บทความหลายชิ้นมีหัวข้อที่นักวิเคราะห์สรุปความเห็นตัวเอง
ต่างจากเนื้อข่าวตรงที่ข่าวเล่าว่าเกิดอะไร ส่วนนี้บอกว่า "แล้วเราคิดว่ายังไง"
**เป็นของที่หาที่อื่นไม่ได้ และเป็นสิ่งที่ RM เอาไปพูดกับลูกค้าได้จริง**

ระบบจึงยกส่วนนี้ขึ้นมาแสดงเด่นทั้งบนเว็บ ในรายงาน และในอีเมล

**ปัญหาที่เจอ** — ตอนแปลง HTML เป็นข้อความ โค้ดแทนทุกแท็กด้วยช่องว่าง
ทำให้โครงสร้างย่อหน้าหายหมด ข้อย่อยที่นักวิเคราะห์แบ่งไว้ 4-5 ข้อกลายเป็นข้อความติดกันเป็นพืด

**วิธีแก้** — แปลงแท็กที่เป็นจุดจบย่อหน้า (`</p>` `</li>` `<br>` ฯลฯ) เป็นบรรทัดใหม่ก่อนลบแท็กอื่น

**การตรวจก่อนแก้ข้อมูลจริง** — ก่อนเปลี่ยนวิธีเก็บเนื้อหาทั้งคลัง
ทดสอบกรณีสุดขั้ว (ทุกช่องว่างกลายเป็นบรรทัดใหม่) กับ 120 บทความ
เทียบผลการวิเคราะห์ก่อน-หลัง: **ต่างกัน 0 รายการ** จึงมั่นใจว่าไม่กระทบส่วนอื่น

**ผลลัพธ์** — 342 บทความมีโครงสร้างย่อหน้าครบ · 68 บทความมีส่วนมุมมอง ·
แยกเป็นข้อย่อยได้ 66 บทความ · จำนวนข้อกลางๆ 4 ข้อ

**การหาจุดจบของส่วน** — ต้นฉบับเขียนเป็นหมวดมีเลขนำ (`6) มุมมองของ InnovestX`)
จุดจบคือหมวดถัดไป (`7)`) หรือหัวข้อ "ข้อสงวนสิทธิ์"
ตอนแรกไม่ได้ดูเลขหมวด ทำให้บทความยาวชิ้นหนึ่งถูกดึงมา **183 ข้อ** แทนที่จะเป็น 4-5 ข้อ

---

## 7. อัลกอริทึมจับคู่

### 7.1 หกระดับความเกี่ยวข้อง

| ระดับ | เงื่อนไข | น้ำหนักฐาน | ผลจริง |
|---|---|---:|---:|
| **L1_HOLD** | ถือหุ้นตัวที่ข่าวพูดถึงอยู่ | 100 | 8,450 (55%) |
| **L2_WATCH** | เคยเทรดใน 90 วัน แต่ตอนนี้ไม่ถือ | 60 | 2,843 (19%) |
| **L3_SECTOR** | ถือหุ้นในกลุ่มอุตสาหกรรมเดียวกัน | 40 | 655 (4%) |
| **L4_RELATED** | ถือหุ้นที่ถูกกล่าวถึงคู่กันบ่อย | 30 | 3,195 (21%) |
| **L5_ASSET** | พอร์ตมีสินทรัพย์ประเภทเดียวกับข่าว | 20 | 188 (1%) |
| **L6_MACRO** | พอร์ตไวต่อประเด็นมหภาคในข่าว | 10 | — |

### 7.2 สูตรคะแนน

```
คะแนน = น้ำหนักฐาน
        × (1 + log-scale ของมูลค่าที่ถือ)
        × ตัวคูณความสำคัญของข่าว
        × ตัวคูณความเร่งด่วน
        × ตัวคูณความสดของธุรกรรม
        × (1 + 0.10 × จำนวนจุดที่แตะเกินหนึ่ง)
```

| ตัวคูณ | ค่า |
|---|---|
| ความเร่งด่วน | ด่วนวันนี้ 1.5 · สัปดาห์นี้ 1.0 · ไม่เร่ง 0.7 |
| ความสดของธุรกรรม | ภายใน 7 วัน 1.3 · ภายใน 30 วัน 1.1 |
| โบนัสหลายจุด | +10% ต่อจุดที่เกินจุดแรก |

**เกณฑ์ตัด** — คะแนนต่ำกว่า 50.0 ไม่เข้ารายการ (R6.14)

**เหตุผลที่ใช้ log-scale กับมูลค่า** — ถ้าใช้มูลค่าตรงๆ ลูกค้ารายใหญ่จะกินรายการทั้งหมด
ทั้งที่ข่าวอาจเกี่ยวกับรายกลางมากกว่า log-scale ทำให้มูลค่ายังมีน้ำหนักแต่ไม่ท่วมปัจจัยอื่น

**คะแนนเฉลี่ยจริงแต่ละระดับ** — L1: 320.3 · L2: 103.5 · L3: 92.7 · L4: 64.7 · L5: 51.7

### 7.3 การจัดลำดับบนหน้าจอ

**จุดที่ตัดสินใจแล้วยึดตลอดโครงการ: เรียงตามเงิน ไม่ใช่คะแนน**

คะแนนบอกว่าข่าวเกี่ยวกับลูกค้าแรงแค่ไหน แต่ไม่บอกว่าคุยแล้วได้เท่าไร

> ลูกค้าคะแนน 51 ที่ถือหุ้นตัวนั้น 31 ล้านบาท **คุ้มกว่า** คะแนน 540 ที่ถือ 3 แสนบาท

จึงเรียงด้วย "เงินที่ข่าวไปแตะ" = มูลค่าที่ลูกค้าถือในตัวที่ข่าวพูดถึง (สลับไปเรียงตามคะแนนได้)

### 7.4 บั๊กการนับมูลค่าซ้ำ

**อาการ** — ยอดเงินรวมโผล่เกินมูลค่าพอร์ตทั้งใบ

**สาเหตุ** — ลูกค้าถือ NVDA อยู่ก้อนเดียว แต่วันนั้นมีข่าวถึง NVDA สามชิ้น
การรวมตรงๆ ได้มูลค่าสามเท่า

**วิธีแก้** — ยุบเป็นราย (ลูกค้า, หุ้น) ด้วย `MAX` ก่อน แล้วค่อยรวมข้ามหุ้น

### 7.5 บั๊กสัญญาณผิดหุ้น

**อาการ** — ADVANC แสดงคำแนะนำว่า *"INVX Research ให้ Outperform กับ **PTTEP** เป้า 170 บาท"*

**สาเหตุ** — บทสรุปรายวันชิ้นเดียวมีคำแนะนำของหุ้นหลายสิบตัว (ของจริงวัดได้ **39 สัญญาณ**)
แต่โค้ดหยิบ `signals[0]` มาใช้กับทุกหุ้น

**วิธีแก้** — เก็บสัญญาณแยกตาม entity แล้วเลือกของหุ้นตัวเอง
รองรับทั้ง `PTTEP` และ `NVDA:xnas` (ตัดรหัสตลาดออกก่อนเทียบ)
**ถ้าไม่มีสัญญาณของตัวเอง เว้นว่าง ไม่ยืมของหุ้นอื่นมาใส่**

### 7.6 การแยกระดับการนำเสนอ

**ปัญหาที่ผู้ใช้รายงาน** — มุมมองตามหุ้นอ่านแล้วไม่สื่อความหมาย

**สาเหตุ** — วันหนึ่ง 14 จาก 35 ตัวโผล่แค่ในสรุปรายวัน
ทุกตัวแสดงพาดหัวเดียวกัน *"คาด SET แกว่งตัว กดดันจากน้ำมัน-Bond Yield ขึ้น"*
ซึ่งไม่ได้บอกอะไรเกี่ยวกับหุ้นตัวนั้น ทั้งลิสต์เลยอ่านเหมือนข่าวเดียวซ้ำสิบกว่าครั้ง

**วิธีแก้** — แยกสองประเภท

| ประเภท | แสดงยังไง |
|---|---|
| มีข่าวเจาะตัวเอง | พาดหัวข่าวขึ้นก่อน เหตุผลรอง |
| ถูกเอ่ยในสรุปรายวันเท่านั้น | **สลับ** — คำแนะนำของหุ้นตัวเองขึ้นก่อน พาดหัวลงเป็นที่มา + ป้าย "จากสรุปรายวัน" |

ผลลัพธ์: `SCCC → INVX Research ให้ Outperform เป้า 200.00 บาท (upside +34%)`
แทนพาดหัวรวมที่ไม่บอกอะไร

---

## 8. การใช้ AI

### 8.1 ทำไมใช้ Claude Code CLI ไม่ใช่ API key

| ประเด็น | ผล |
|---|---|
| ไม่ต้องมี `ANTHROPIC_API_KEY` ในระบบ | ไม่มีความลับให้รั่ว |
| ใช้สิทธิ์ที่ผู้ใช้ล็อกอินอยู่แล้ว | ไม่มีบิลแยกต่อ token |
| เรียกผ่าน `claude -p` (headless) | ฝังในเว็บตรงจุดที่ใช้ได้เลย |

**การส่ง prompt** — ส่งผ่าน **stdin ไม่ใช่ argv**
เพราะเนื้อข่าวมีอักขระพิเศษเยอะ และบน Windows คำสั่งต้องผ่าน `cmd.exe`
(ตัว `claude` เป็น `.cmd` shim) ถ้าใส่ใน command line จะโดนตีความผิด

### 8.2 การลดต้นทุน token

`claude -p` แบบไม่ตั้งค่าจะแบก system prompt ของผู้ช่วยเขียนโค้ดกับคำอธิบายเครื่องมือทั้งชุดไปด้วย

| | token ต่อครั้ง |
|---|---:|
| เรียกแบบปกติ | 32,485 |
| ตั้ง system prompt สั้น + ปิดเครื่องมือทั้งหมด | **8,336** |

**ลดลง 4 เท่า** โดยผลลัพธ์เท่าเดิม เพราะงานนี้ไม่ได้ใช้เครื่องมือใดเลย

### 8.3 ด่านตรวจผลลัพธ์ — หลักการสำคัญที่สุดของโครงการ

> **ผลจากโมเดล "เสนอ" ได้ แต่ต้องผ่านการตรวจก่อนถูกบันทึก**

ทุกสิ่งที่ AI เสนอต้องมี **ประโยคจากบทความจริงกำกับ** และผ่านด่าน:

1. ประโยคที่ยกมาต้องมีอยู่ในบทความจริง (`verify_quote`)
2. ตัวย่อหุ้นที่เสนอต้องมีคนถืออยู่จริงในพอร์ตลูกค้า
3. ไม่ผ่านด่าน → ลงตาราง `unmapped` ให้คนดู ไม่ใช่ทิ้งเงียบ

**เหตุผล** — หน้าจอสัญญากับ RM ว่าทุกแถวอ้างหลักฐานได้ ไม่ใช่ "โมเดลว่ามา"

### 8.4 ผลการใช้งานจริง

| | จำนวน |
|---|---:|
| บทความที่ AI อ่านแล้ว | 149 |
| ฟันธงทิศทางได้ | 21 |

**สาเหตุที่ไม่ฟันธง** (บันทึกไว้ทุกกรณี ไม่ปล่อยเป็นช่องว่าง)

| เหตุผล | จำนวน |
|---|---:|
| `other` | 39 |
| `bad_answer` — ตอบผิดรูปแบบ | 38 |
| `not_about_stock` — ไม่ได้เกี่ยวกับหุ้นรายตัว | 23 |
| `conflicting` — เนื้อหาขัดกันเอง | 14 |
| `data_only` — มีแต่ตัวเลข ไม่มีความเห็น | 7 |
| `quote_failed` — ยกประโยคไม่ผ่านการตรวจ | 1 |

**การตีความตัวเลขนี้** — อัตราฟันธง 14% ดูต่ำ แต่เป็นผลที่ตั้งใจ
ระบบเลือกที่จะตอบ "ไม่ฟันธง" มากกว่าจะเดา
และ **"อ่านแล้วไม่ฟันธง" ถูกแยกจาก "ยังไม่ได้อ่าน"** ชัดเจนบนหน้าจอ
เพราะสองอย่างนี้ต้องทำต่อคนละแบบ

### 8.5 การแสดงผลของ AI บนหน้าจอ

แยกกล่องคนละสีกับความเห็นของนักวิเคราะห์ **โดยตั้งใจ**

| | สี | ที่มา |
|---|---|---|
| มุมมองของ InnovestX | น้ำเงิน | นักวิเคราะห์ของบริษัทเขียนเอง |
| AI อ่านแล้วสรุปว่า | ม่วง | เครื่องอ่านเนื้อข่าวแล้วสรุป |

น้ำหนักไม่เท่ากัน ต้องไม่ถูกอ่านสลับกัน
กล่อง AI แสดงทิศทางเป็นคำไทยพร้อมสี + **ประโยคที่ยกมาเป็นหลักฐาน**
ซึ่ง RM เอาไปอ้างกับลูกค้าได้ตรงๆ

---

## 9. ฟีเจอร์ปันผล

### 9.1 ที่มาของข้อมูล

INVX Data Book รายเดือน — PDF ที่มีตาราง "ตามรอยหุ้นปันผล"

**การตัดสินใจ: ไม่ใช้ AI อ่าน**
ตารางนี้มีรูปแบบตายตัวทุกบรรทัด อ่านด้วยกฎได้ตรงๆ
ใช้ AI = เพิ่มโอกาสพลาดฟรีๆ กับตัวเลขที่ RM เอาไปคุยกับลูกค้า

**การดึงอัตโนมัติ** — ระบบหา PDF เดือนล่าสุดเองผ่าน API เดียวกับข่าว
(`sub_category_slug=monthly-report` — พบย้อนหลัง 47 ฉบับ)
แล้วหาหน้าที่มีตารางจากเนื้อหา **ไม่ยึดเลขหน้า** เพราะเลขหน้าขยับทุกเดือน

### 9.2 ด่านตรวจตัวเลข

ตัวเลขการเงินอ่านผิดแล้วเสียหายจริง จึงตรวจด้วยความสัมพันธ์ทางคณิตศาสตร์ **ไม่ต้องเชื่อตัวแกะ**

**ด่านที่ 1: `dps ÷ price` ต้องเท่ากับ `% ผลตอบแทน` ที่รายงานเขียน**

สมการเดียวยืนยันสามช่องพร้อมกัน อ่านเพี้ยนช่องไหนก็ไม่ผ่าน

ทดสอบกับของจริง: CPF `0.80 ÷ 22.50 = 3.55 ≈ 3.6` ✓ · PTTEP `4.50 ÷ 149 = 3.02 ≈ 3.0` ✓

**ปัญหาที่เจอ** — SIRI ตกด่าน: `0.05 ÷ 1.51 = 3.31%` แต่รายงานเขียน `3.0`
ไม่ใช่ข้อมูลผิด แต่รายงานปัดเศษปันผลเหลือ 2 ตำแหน่ง หุ้นราคาถูกจึงคลาดเยอะ

**วิธีแก้** — ค่าคลาดเคลื่อนที่ยอมรับได้คำนวณจากจำนวนทศนิยมที่แสดงจริง
ไม่ใช่ค่าคงที่ที่ตั้งเอง → ผ่านครบ **78/78 แถว**

**ด่านที่ 2: `ปันผลทั้งปี ≥ ปันผลงวดนี้`** — จับกรณีคอลัมน์สลับ (ตรวจกับตัวอย่าง 8 แถวผ่านหมด)

### 9.3 การคำนวณเงินปันผลรายคน

```
เงินที่ลูกค้าจะได้ = มูลค่าที่ถือ × % ผลตอบแทน
```

ถูกต้องเพราะ `จำนวนหุ้น = มูลค่าที่ถือ ÷ ราคา` และ `ปันผล = จำนวนหุ้น × ปันผลต่อหุ้น`
จึงไม่ต้องรู้จำนวนหุ้นเลย

**ผลจริง** — หุ้นในรายงาน 78 ตัว **ลูกค้าถือจริง 75 ตัว**
รวมเงินปันผลที่ลูกค้าจะได้รอบนี้ **19,709,317 บาท**

### 9.4 สไตล์พอร์ต — บทเรียนเรื่องความซื่อสัตย์ของข้อมูล

จำแนกลูกค้าด้วยสองแกน: yield ของพอร์ต × ความถี่เทรด

| | เทรดน้อย | เทรดบ่อย |
|---|---|---|
| **yield สูง** | นักลงทุนปันผล | เก็บปันผลแบบเทรด |
| **yield ต่ำ** | ถือยาวหวังราคา | เก็งกำไร |

เส้นแบ่ง yield ใช้**ค่ากลางของฐานลูกค้าเองที่คำนวณสด** (4.10%) ไม่ใช่เลขที่ตั้งเอง
เพื่อให้อธิบายได้ว่า "สูงกว่าครึ่งหนึ่งของลูกค้าที่วัดได้"

**ผลลัพธ์จริง — และการตัดสินใจที่สำคัญ**

| สไตล์ | จำนวน |
|---|---:|
| **ยังบอกไม่ได้** | **947** |
| เก็บปันผลแบบเทรด | 50 |
| เก็งกำไร | 44 |
| ถือยาวหวังราคา | 10 |
| นักลงทุนปันผล | 6 |

> **947 คนที่ "ยังบอกไม่ได้" คือคำตอบที่ถูกต้อง ไม่ใช่บั๊ก**

Data Book ครอบคลุมแค่หุ้นไทย แต่ลูกค้าเกือบครึ่งเป็น US_OFFSHORE
ถ้าแปะป้ายให้ทุกคนจะได้ "เก็งกำไร" เกลื่อนจอ ทั้งที่ความจริงคือ
**เราไม่มีข้อมูลปันผลของสิ่งที่เขาถือ** — คนละเรื่องกับการรู้ว่าเขาถือของที่ไม่จ่ายปันผล

จึงกำหนดว่าต้องมีข้อมูลครอบคลุมเกิน 30% ของพอร์ตถึงจะสรุป (ผ่านเกณฑ์ 110 คน)
และหน้าจอบอกเหตุผลตรงๆ ว่าครอบคลุมเท่าไรและทำไมถึงยังสรุปไม่ได้

---

## 10. การส่งอีเมล

### 10.1 โจทย์

หัวหน้าทีมเป็นคนใส่ข้อมูลแล้วส่งให้ RM แต่ละคนรู้ว่าวันนี้ควรโทรหาใคร

### 10.2 การตัดสินใจ: Outlook COM ไม่ใช่ SMTP

เวอร์ชันแรกทำด้วย SMTP แล้วเปลี่ยนทีหลัง หลังพบว่าโปรเจกต์พี่น้องในองค์กรใช้ Outlook COM สำเร็จแล้ว

| | SMTP | **Outlook COM** |
|---|---|---|
| รหัสผ่าน | ต้องเก็บ (เสี่ยงรั่ว) | **ไม่ต้องมีเลย** |
| ขอจากฝ่าย IT | ต้อง | ไม่ต้อง |
| ประวัติการส่ง | ไม่มี | อยู่ใน Sent Items ตามปกติ |
| ข้อจำกัด | ใช้ได้ทุก OS | Windows เท่านั้น |

**รายละเอียดที่ห้ามเปลี่ยน** (เหตุผลมาจากของที่พังมาก่อน)

1. **ต้องใช้ `GetActiveObject` ไม่ใช่ `New-Object`** — instance ที่ COM สร้างเองมักข้ามขั้นตอน login แล้ว `.Send()` เงียบๆ โดยไม่มีอะไรออกไปจริง
2. **ข้อความส่งผ่านไฟล์ JSON + environment variable** ไม่แทรกเป็นซอร์ส PowerShell — ชื่อ/หัวข้อที่มีอักขระพิเศษจึงแทรกคำสั่งไม่ได้
3. **`Get-Content` ต้องระบุ `-Encoding UTF8`** — Windows PowerShell 5.1 อ่านเป็น cp874 โดยปริยาย ภาษาไทยเพี้ยนทั้งฉบับ
4. **แตะ `.Session.CurrentUser` ยืนยันอีกชั้น** — บางครั้ง `GetActiveObject` สำเร็จตั้งแต่ profile ยังไม่พร้อม แล้วไปพังตอน `.Send()` ซึ่งสายเกินจะย้อน

### 10.3 การลองซ้ำอัตโนมัติ

ลองสูงสุด 3 รอบ รอบละรอ Outlook พร้อมสูงสุด ~100 วินาที

**หลักที่สำคัญ: ลองซ้ำเฉพาะกรณีที่ยืนยันได้ว่ายังไม่มีอะไรส่งออกไป**
ถ้าพังหลัง `.Send()` หยุดทันที ไม่ลองซ้ำ เพราะอาจส่งไปแล้ว
การลองใหม่จะกลายเป็นส่งซ้ำสองฉบับ

### 10.4 รูปแบบรายงาน

**ตัดสินใจ: แนบไฟล์ HTML อย่างเดียว ไม่แนบ CSV**

เดิมแนบทั้งสองอย่าง แต่ CSV เปิดมาเจอตัวเลขดิบ 300 แถว อ่านไม่ออกว่าควรโทรใครก่อน

ไฟล์ HTML เป็น **ไฟล์เดียวจบ ไม่มี JavaScript ไม่มีลิงก์ออกนอก**
(ไฟล์แนบที่มีสคริปต์มักโดนนโยบายความปลอดภัยขององค์กรบล็อก)

**เนื้อหา**
- รายการหุ้นเรียงตามเงิน + แถบเทียบสัดส่วน
- ป้ายทิศทางข่าว · ป้าย "InnovestX Daily Top Pick" · ป้าย "จากสรุปรายวัน"
- ลิงก์เปิดข่าวตัวเต็ม
- กล่องมุมมองของ InnovestX (บุลเล็ต) และกล่อง AI (พร้อมประโยคอ้างอิง)
- **กดที่รหัสลูกค้า → เปิดหน้าของคนนั้นในไฟล์เดียวกัน**

**หน้าลูกค้าในไฟล์** ทำด้วย CSS `:target` ล้วน แสดง:
สถิติ 5 ตัว · สัดส่วนสินทรัพย์ · กลุ่มอุตสาหกรรม · รายการถือครอง · ธุรกรรมล่าสุด ·
watchlist · เหตุผลรายคน · ปันผล

**ขนาดจริง** — RM001 549 KB (57 หน้าลูกค้า) · รวมทั้งอีเมล 5 ไฟล์ 2.6 MB

### 10.5 ด่านกันความผิดพลาด

อีเมลส่งแล้วเรียกคืนไม่ได้ จึงมีด่านหลายชั้น:

| ด่าน | ทำอะไร |
|---|---|
| ต้องดูตัวอย่างจริงก่อน | หน้าตัวอย่างใช้ได้โดยไม่ต้องตั้งค่าอะไร |
| ไม่มีโหมด "ส่งทุกคน" โดยปริยาย | ไม่ระบุผู้รับ → ปฏิเสธ |
| ตรวจอีเมลก่อนแตะ Outlook | ขาดอีเมลใคร → แจ้งชื่อก่อน ไม่ส่งบางส่วนแล้วค่อยบอก |
| ขั้นยืนยันไล่รายชื่อจริง | บอกจำนวนฉบับ ไฟล์แนบ และอีเมลทุกบรรทัด |
| **ปฏิเสธถ้าข้อมูลไม่ใช่ของวันนี้** | ต้องยืนยันซ้ำ (`send_stale`) |

**ด่านสุดท้ายมาจากปัญหาจริง** — ถ้าเช้านี้ยังไม่มีใครกดดึงข่าว
ระบบจะใช้ข่าวของเมื่อวานโดยที่หน้าจอไม่ได้ผิดอะไร
แล้วอีเมลที่ส่งออกไปคือคิวโทรของเมื่อวานทั้งฉบับ
ด่านนี้อยู่ฝั่งเซิร์ฟเวอร์ **ไม่ใช่แค่ป้ายเตือนบนหน้าจอ** เพราะป้ายเตือนถูกมองข้ามได้

---

## 11. หน้าจอ

13 หน้า แบ่งสองกลุ่ม

### 11.1 กลุ่มที่ RM ใช้ทุกวัน

| หน้า | ตอบคำถาม |
|---|---|
| **งานวันนี้ของ RM** (หน้าแรก) | วันนี้ต้องคุยเรื่องอะไรกับใคร |
| หาจากหุ้น | หุ้นตัวนี้มีใครถือ ข่าวว่าไง |
| จังหวะรายวัน | เช้า/ระหว่างวัน/เย็น มีอะไรบ้าง |
| ข่าวทั้งหมด | ค้นข่าวย้อนหลัง (กรองตามวันที่ได้) |
| ตามรอยหุ้นปันผล | เดือนนี้ใครได้ปันผลเท่าไร |
| ลูกค้า | ดูพอร์ตรายคน |

### 11.2 กลุ่มผู้ดูแล

นำเข้าไฟล์ · **ส่งอีเมลให้ RM** · รายงาน · Spec · ตั้งค่า

หน้าส่งอีเมลถูกแยกออกมาเป็นปุ่มเด่นในเมนู เพราะเป็น **การกระทำ ไม่ใช่หน้าไว้ดู**
และเป็นปลายทางของงานทั้งวัน (ใส่ข้อมูล → ดูคิว → ส่งให้ทีม)

### 11.3 มุมมองสามแบบในหน้าแรก

| มุม | เหมาะกับ |
|---|---|
| **ตามหุ้น** (ค่าเริ่มต้น) | โทรทีละหุ้น ได้ประเด็นเดียวคุยได้หลายคน |
| ตามลูกค้า | ไล่รายชื่อทีละคน |
| ตามข่าว | ดูว่าข่าวชิ้นไหนกระทบเงินมากสุด |

**ตามหุ้นเป็นค่าเริ่มต้นเพราะ** เรียงตามลูกค้าต้องเปลี่ยนเรื่องพูดทุกสาย

---

## 12. หลักการออกแบบที่ยึดตลอด

### 12.1 ห้ามเงียบ

ทุกอย่างที่ระบบจัดการไม่ได้ต้องเข้าบัญชี `unmapped` พร้อมเหตุผลและเลขกฎ
ไม่มีการ `except: pass` ที่กลืนปัญหา

### 12.2 ทุกบรรทัดต้องอธิบายที่มาได้

ผลจับคู่ทุกแถวเก็บ `evidence` เป็น JSON ที่บอก:
- แตะระดับไหนบ้าง (hits)
- ตัวคูณแต่ละตัวเป็นเท่าไร (factors)
- ประโยคที่ทำให้จับได้

หน้าจอสัญญากับ RM ว่าอ้างหลักฐานได้เสมอ

### 12.3 ไม่มั่นใจ ไม่เดา

- แปลงรหัสไม่ได้ → `unmapped` ไม่ใช่เดาตัวใกล้เคียง
- AI ฟันธงไม่ได้ → บอกว่าไม่ฟันธงพร้อมเหตุผล
- ข้อมูลไม่พอสรุปสไตล์ → บอกว่า "ยังบอกไม่ได้" พร้อมบอกว่าครอบคลุมเท่าไร
- ไม่มีสัญญาณของหุ้นตัวเอง → เว้นว่าง ไม่ยืมของหุ้นอื่น

### 12.4 เรียงตามมูลค่า ไม่ใช่จำนวน

ทุกตารางเรียงด้วยเงิน ลูกค้าคนเดียวที่ได้ปันผลล้านบาทสำคัญกว่าสิบคนที่ได้คนละพัน

### 12.5 ค่าเริ่มต้นคือภาพรวม

ระบบมีผู้ใช้สองกลุ่ม (RM ในทีม / คนนอกทีม) ทุกหน้าจึงเริ่มที่ภาพรวมแล้วค่อยกรองลง

### 12.6 แยก "ประมาณการ" จาก "ยืนยันแล้ว"

ตัวเลขปันผลที่ยังไม่ประกาศต้องเขียนกำกับว่า "คาดการณ์" ทุกที่ที่โผล่
RM ต้องไม่พูดเหมือนเป็นตัวเลขที่ยืนยันแล้ว

---

## 13. ตัวเลขผลลัพธ์

### 13.1 ความครอบคลุม

| ตัวชี้วัด | ค่า |
|---|---:|
| ลูกค้าในระบบ | 1,106 |
| รายการถือครอง | 15,369 |
| ธุรกรรมย้อนหลัง | 156,442 (6.3 เดือน) |
| บทความในคลัง | 355 |
| ผลจับคู่ | 15,331 |
| หุ้นนอกที่เติม sector ได้ | 2,396 / 3,181 (75%) |
| หุ้นปันผลที่ลูกค้าถือ | 75 / 78 (96%) |

### 13.2 ตัวอย่างผลลัพธ์วันหนึ่ง (RM001)

| | ค่า |
|---|---:|
| หุ้นที่ข่าววันนั้นแตะ | 43 ตัว |
| รายชื่อลูกค้าที่ต้องคุย | 294 |
| เงินที่ข่าวไปแตะ | 518 ล้านบาท |
| ทั้งทีม 5 คนรวม | 2,429 ล้านบาท |

### 13.3 คุณภาพ

| | ค่า |
|---|---:|
| บทความที่หลักฐานแน่น (`confirmed`) | 279 / 350 (80%) |
| บทความที่หลักฐานบาง (`weak`) | 6 (1.7%) |
| รายการที่ระบบจัดการไม่ได้ | 518 แบบ (บันทึกครบ) |

---

## 14. ปัญหาที่เจอและวิธีแก้

สรุปรวมปัญหาสำคัญตลอดโครงการ

| # | ปัญหา | สาเหตุ | วิธีแก้ |
|---|---|---|---|
| 1 | ยอดเงินเกินมูลค่าพอร์ต | ข่าวหลายชิ้นชี้หุ้นเดียวกัน รวมซ้ำ | ยุบราย (ลูกค้า,หุ้น) ด้วย MAX ก่อนรวม |
| 2 | ADVANC ได้คำแนะนำของ PTTEP | หยิบ `signals[0]` ใช้กับทุกหุ้น | เก็บสัญญาณแยกตาม entity |
| 3 | มุมมองนักวิเคราะห์ติดกันเป็นพืด | แปลง HTML ทำโครงสร้างย่อหน้าหาย | แปลงแท็กจบย่อหน้าเป็นบรรทัดใหม่ |
| 4 | ดึงมุมมองมา 183 ข้อ | ไม่ได้ดูเลขหมวดเป็นจุดจบ | ใช้หมวดถัดไปเป็นขอบเขต + เพดาน 12 ข้อ |
| 5 | Yahoo ตอบ `Invalid Crumb` | ต้องมี cookie + crumb | ขอ cookie จาก `fc.yahoo.com` (ซึ่งตอบ 404 แต่แนบ cookie มา) |
| 6 | SIRI ตกด่านตรวจปันผล | รายงานปัดเศษ หุ้นราคาถูกคลาดเยอะ | ค่าคลาดเคลื่อนคิดจากทศนิยมที่แสดงจริง |
| 7 | ภาษาไทยเพี้ยนใน Excel/PowerShell | Windows ใช้ cp874 โดยปริยาย | CSV ใส่ BOM · PowerShell ระบุ `-Encoding UTF8` |
| 8 | ตัวเลข AI ขยับช้าจนดูเหมือนค้าง | นับทั้งคลัง แต่ปุ่มสั่งอ่านเฉพาะวันนี้ | นับเฉพาะวันเดียวกับที่ปุ่มทำ + poll ระหว่างอ่าน |
| 9 | หุ้นเด่นไม่ขึ้นป้าย | อ่านจากบทความ lead ตัวเดียว | ดูข่าวทุกชิ้นของวันนั้น |
| 10 | กราฟ/ข่าวไม่อัปเดตรายวัน | ไม่มี scheduler ต้องกดเอง | ยังไม่แก้ (ดูหัวข้อ 15) |

---

## 15. ข้อจำกัดที่ยังเหลือ

| ข้อจำกัด | ผลกระทบ | ทางแก้ที่เสนอ |
|---|---|---|
| **ไม่มีการอัปเดตอัตโนมัติ** | ต้องกดปุ่มดึงข่าวเอง ลืมแล้วข้อมูลค้าง | ตั้ง Windows Scheduled Task หรือใส่ scheduler ใน backend |
| ส่งอีเมลได้เฉพาะ Windows | เครื่อง Mac/Linux ใช้ไม่ได้ | ต้องกลับไปทำ SMTP ถ้าจำเป็น |
| sector หุ้นนอกยังไม่ครบ | 785 ตัวไม่มี sector (ETF + ตลาดที่ไม่รองรับ) | เพิ่ม mapping ตลาด · หาแหล่งข้อมูล ETF |
| สไตล์พอร์ตวัดได้ 110 จาก 1,106 คน | ลูกค้าส่วนใหญ่สรุปสไตล์ไม่ได้ | ต้องมีข้อมูลปันผลหุ้นต่างประเทศ |
| AI ฟันธงได้ 21 จาก 149 บทความ | ต้องพึ่งกฎเป็นหลัก | ปรับ prompt · เพิ่มบทความที่มีเนื้อหาเต็ม |
| ไม่มีระบบผู้ใช้ | ใครเปิดโปรแกรมก็เห็นทุกอย่าง | ยอมรับได้เพราะรันบนเครื่องผู้ดูแล |
| PDF บางฉบับอ่านไม่ได้ | 26 บทความเป็น PDF ล้วน ไม่มีข้อความบนเว็บ | ต้องแกะ PDF เพิ่ม |

---

## 16. สิ่งที่ได้เรียนรู้

### 16.1 ด้านเทคนิค

**การจัดการข้อมูลจริงยากกว่าที่คิด**
รหัสสินทรัพย์ตัวเดียวเขียนได้หลายแบบ ฮ่องกงเติมศูนย์ ไทยไม่มี suffix
สหรัฐฯ มี MIC ต่อท้าย ทุกรูปแบบต้องรองรับหมดโดยไม่เดาผิด

**การเข้ารหัสอักขระเป็นปัญหาจริงบน Windows**
เจอ cp874 ทำภาษาไทยเพี้ยนถึง 3 จุดคนละบริบท: Excel เปิด CSV · PowerShell อ่านไฟล์ ·
คอนโซลพิมพ์ผลลัพธ์ แต่ละจุดแก้คนละวิธี

**การทดสอบก่อนแก้ข้อมูลจริง**
ก่อนเปลี่ยนวิธีเก็บเนื้อหาทั้งคลัง จำลองกรณีสุดขั้วแล้วเทียบผลก่อน-หลัง 120 บทความ
ได้ผลต่าง 0 จึงมั่นใจว่าปลอดภัย — ถ้าไม่ทดสอบก่อนอาจพังทั้งระบบโดยไม่รู้ตัว

### 16.2 ด้านการออกแบบ

**"ไม่รู้" เป็นคำตอบที่ถูกต้อง**
บทเรียนที่ชัดที่สุดคือเรื่องสไตล์พอร์ต — 947 คนที่ระบบตอบว่า "ยังบอกไม่ได้"
ถูกต้องกว่าการแปะป้ายให้ครบทุกคน
ระบบที่ตอบทุกคำถามได้เสมอมักเป็นระบบที่เดา

**ป้ายเตือนบนหน้าจอไม่พอ**
ด่านกันส่งข้อมูลเก่าต้องอยู่ฝั่งเซิร์ฟเวอร์ ไม่ใช่แค่ข้อความสีเหลือง
เพราะข้อความเตือนถูกมองข้ามได้ แต่ HTTP 409 ถูกมองข้ามไม่ได้

**แยกแหล่งที่มาให้ชัด**
ความเห็นนักวิเคราะห์ (คนเขียน) กับผลจาก AI (เครื่องอ่าน) ต้องแยกกล่องคนละสี
เพราะน้ำหนักไม่เท่ากัน ถ้าผสมกันคนอ่านจะเชื่อผิดระดับ

### 16.3 ด้านกระบวนการทำงาน

**ความผิดพลาดที่เกิดขึ้นจริงในโครงการ**
ระหว่างทดสอบด่านกันข้อมูลเก่า มีการเรียก API ส่งอีเมลจริงเพื่อดูว่าจะถูกปฏิเสธไหม
แต่วันนั้นข้อมูลสด ด่านจึงไม่ทำงาน แล้วอีเมลถูกส่งออกไปจริงพร้อมไฟล์แนบที่มีข้อมูลพอร์ต

**บทเรียน** — ฟังก์ชันที่ส่งของออกนอกเครื่องห้ามเรียกเพื่อทดสอบ
ต้องทดสอบผ่านเส้นทางที่ไม่ส่งจริง (preview) เท่านั้น
บันทึกไว้เป็นข้อห้ามใน `SETUP.md` แล้ว

**การอ่านโค้ดที่มีอยู่ก่อนเขียนใหม่**
ฟีเจอร์ส่งอีเมลเวอร์ชันแรกทำด้วย SMTP ใช้เวลาไปพอสมควร
แล้วพบทีหลังว่าโปรเจกต์พี่น้องในองค์กรใช้ Outlook COM สำเร็จแล้ว
ซึ่งดีกว่าทุกด้าน — ถ้าถามหาของที่มีอยู่ก่อนจะประหยัดเวลาไปมาก

---

## ภาคผนวก — รายการ API

40 endpoint แบ่งตามหน้าที่

**ข้อมูลระบบ** — `GET /api/health` · `GET /api/reference` · `GET /api/spec-gaps`

**ข่าว** — `GET /api/articles` · `GET /api/articles/{id}` · `GET /api/articles/{id}/briefing` ·
`GET /api/articles/{id}/matches` · `GET /api/today` · `GET /api/dates` · `POST /api/ingest/news`

**ลูกค้า** — `GET /api/customers` · `GET /api/customers/{key}` ·
`POST /api/upload` · `POST /api/upload/check` · `POST /api/ingest/customers` · `GET /api/mask-guide`

**หุ้น** — `GET /api/entities` · `GET /api/entities/{entity}` ·
`GET /api/entities/{entity}/chart` · `GET /api/entities/highlights`

**มุมมอง RM** — `GET /api/rms` · `GET /api/rm/{rm}/queue` ·
`GET /api/rm/{rm}/news` · `GET /api/rm/{rm}/entities`

**จับคู่** — `POST /api/match`

**AI** — `GET /api/ai/health` · `POST /api/ai/enrich`

**ปันผล** — `GET /api/dividends` · `GET /api/dividends/styles` · `POST /api/ingest/dividends`

**อีเมล** — `GET /api/mail/config` · `PUT /api/mail/config` ·
`GET /api/mail/preview` · `GET /api/mail/file` · `POST /api/mail/send`

**รายงาน** — `GET /api/reports/unmapped` · `GET /api/reports/coverage` ·
`GET /api/reports/related` · `GET /api/reports/verification`

---

*เอกสารนี้สร้างเมื่อ 7 สิงหาคม 2569 · ตัวเลขทั้งหมดดึงจากระบบจริง*
