# match-port — Engineering Handover

เอกสารนี้เขียนไว้ให้คนที่ไม่เคยเปิดโค้ดนี้มาก่อน อ่านแล้วต่องานได้เลย ครอบคลุมทุกหน้าเว็บ
ทุกโมดูล flow การทำงาน และสถาปัตยกรรมทั้งระบบ ตัวอย่างและตัวเลขในเอกสารมาจากสถานะจริง
ของฐานข้อมูล ณ วันที่เขียน (2026-08-04) ไม่ใช่ค่าสมมติ

> เอกสารอีกฉบับในโปรเจกต์คือ `HANDOVER-PITCH.md` — นั่นคือของนำเสนอ ไม่ใช่ของทางเทคนิค
> ถ้าต้องการเข้าใจโค้ด/สถาปัตยกรรม/วิธีต่องาน ให้อ่านไฟล์นี้

---

## 1. ระบบนี้ทำอะไร

**match-port** (ชื่อโปรเจกต์ภายใน INVX) เป็นระบบที่ตอบคำถามเดียว: **"วันนี้ RM ควรโทรหาใคร
และทำไม"** โดยจับคู่ข่าว/บทวิเคราะห์จากเว็บ Cafe Invest ของ INVX กับพอร์ตการลงทุนจริงของ
ลูกค้า แล้วจัดอันดับว่าใครควรได้รับการติดต่อก่อน พร้อมเหตุผลที่อ้างอิงได้ทุกแถว (ไม่ใช่
คะแนนลอย ๆ)

ผู้ใช้มีสองกลุ่ม (บันทึกไว้ในความจำของผู้ช่วย AI ด้วย):
1. **RM ในทีม** — อยากรู้ว่าวันนี้ควรโทรใคร เปิดหน้า "วันนี้" หรือ "รายชื่อที่ต้องโทร"
2. **คนนอกทีมที่ดูภาพรวม** — อยากเห็นภาพรวมทั้งบริษัท ไม่ได้ดูแลพอร์ตเอง หน้า "หาจากหุ้น"
   (`/`) เป็นหน้าแรกที่เปิด และค่าเริ่มต้นทุกหน้าคือภาพรวม ไม่กรองด้วยผู้ดูแลคนใดคนหนึ่ง

ระบบสร้างขึ้นจากเอกสาร spec ภายใน 8 ชุด (STEP1–STEP8) รวม 136 กฎ ที่ระบุไว้เป็นตัวเลขกฎ
กำกับในโค้ดทุกจุด (เช่น `R6.1`, `R3.38`) เพื่อให้ตรวจสอบย้อนกลับได้ว่าโค้ดบรรทัดไหนมาจาก
ข้อกำหนดข้อไหน จุดที่ spec เขียนไม่ครบหรือขัดกับข้อมูลจริงถูกบันทึกเป็น `GAP-01..GAP-23`
ใน `backend/app/tables.py` ท้ายไฟล์ และแสดงบนหน้าเว็บที่ `/spec`

หลักการที่คุมทั้งระบบ (ปรากฏซ้ำ ๆ ในทุกโมดูล): **"ไม่มั่นใจ ไม่เดา — ให้รายงานแทน"**
ทุกจุดที่ระบบแปลง/สกัด/จับคู่อะไรไม่ได้ จะถูกบันทึกลงตาราง `unmapped` แทนที่จะทิ้งเงียบ ๆ
หรือเดาค่าใส่ไป

---

## 2. สถาปัตยกรรมโดยรวม

```
┌─────────────────────────────────────────────────────────────────────┐
│  Cafe Invest API (เว็บข่าว INVX)        Yahoo Finance (ราคาหุ้นไทย)   │
│  TradingView widget (ราคาหุ้นนอก/คริปโต)                              │
└───────────────┬───────────────────────────────┬─────────────────────┘
                │ news.py                        │ prices.py / symbols.py
                ▼                                 ▼
┌─────────────────────────────────────────────────────────────────────┐
│                    backend/  (FastAPI + SQLite, ไฟล์เดียว)            │
│                                                                       │
│   ไฟล์ Excel ลูกค้า (data/*.xlsx)                                     │
│         │ ingest_customers.py + mapping.py (STEP1/4/5)               │
│         ▼                                                            │
│   customers / holdings / transactions                                │
│                                                                       │
│   ข่าวดิบจาก API                                                      │
│         │ news.py: tag() (STEP2/3) → verify.py: grade() (STEP8)      │
│         ▼                                                            │
│   articles (+ segments ของ Brief)                                    │
│         │                                                             │
│         │ enrich.py (AI-01/AI-02) — Claude Code CLI อ่านเนื้อหาเต็ม   │
│         ▼                                                            │
│   articles.entity / ai_direction / ai_reason (เติมเพิ่ม ไม่ทับของกฎ)   │
│         │                                                             │
│         ▼                                                            │
│   matching.py: match_article() (STEP6) → matches                    │
│         │                                                             │
│         ▼                                                            │
│   briefing.py: analyse() (GAP-22) — ทิศทาง + ประเด็นคุย                │
│         │                                                             │
│   main.py — FastAPI routes (/api/*)                                  │
└───────────────┬───────────────────────────────────────────────────────┘
                │ REST + JSON
                ▼
┌─────────────────────────────────────────────────────────────────────┐
│              frontend/  (React + TypeScript + Vite + TanStack Query) │
│  11 หน้า: Stock(หน้าแรก) · Today · News · Article · RmQueue ·         │
│           Customers · Customer · Reports · Spec · Settings · Upload  │
└─────────────────────────────────────────────────────────────────────┘
```

**ทำไมเป็นสถาปัตยกรรมนี้:**
- SQLite ไฟล์เดียว (`backend/matchport.db`, WAL mode) — ไม่ต้องติดตั้ง DB server แยก รันบน
  เครื่อง RM ได้ทันทีด้วย `start.ps1` / `launch.ps1` (ดูข้อ 9)
- ผลจับคู่ (`matches`) **คำนวณล่วงหน้าตอน ingest** ไม่ใช่ query สด — หน้าเว็บเปิดเร็วเพราะ
  ไม่ต้องรันกฎการจับคู่ทั้งชุดตอนโหลดหน้า
- ไม่มีคิว/worker แยก งานเบื้องหลังทั้งหมด (ดึงข่าว, ให้ AI อ่าน, จับคู่ใหม่) เรียกตรงจาก
  HTTP endpoint แบบ synchronous เพราะ scale ของงาน (~1,100 ลูกค้า, ~300 บทความ/วัน) เล็ก
  พอที่จะไม่ต้องทำ async job queue

---

## 3. โครงสร้างโฟลเดอร์

```
match-port/
├── start.ps1 / launch.ps1     รันระบบทั้งหมด (ดูข้อ 9)
├── data/                       ไฟล์ Excel ลูกค้า (portfolio_masked.xlsx, txn_masked.xlsx)
│   └── archive/                สำเนาไฟล์ชุดก่อนหน้าทุกครั้งที่อัปโหลดใหม่
├── backend/
│   ├── app/                    โค้ดหลัก (import เป็น package "app")
│   │   ├── main.py             FastAPI — ทุก endpoint (/api/*)
│   │   ├── db.py               SQLite schema + migration + helper (jload/jdump)
│   │   ├── tables.py           "กฎ" ทั้งหมดเป็นค่าคงที่ในโค้ด (STEP1-STEP8) + SPEC_GAPS
│   │   ├── mapping.py          STEP4 — แปลงรหัสสินทรัพย์ทุกชนิดเป็น "รหัสกลาง"
│   │   ├── news.py             STEP2/3 — ดึงข่าว, แท็ก 23 field, ซอย Brief
│   │   ├── verify.py           STEP8 — ให้เกรดคุณภาพ entity ที่สกัดเอง (ไม่บล็อกการจับคู่)
│   │   ├── matching.py         STEP6 — คำนวณคะแนน L1-L6 ต่อ (บทความ, ลูกค้า)
│   │   ├── briefing.py         GAP-22 — ทิศทางข่าว + ประเด็นที่ควรคุย (ไม่ใช้ AI)
│   │   ├── enrich.py           AI-01/AI-02 — ให้ Claude Code อ่านเนื้อหาเต็ม (เขียนใหม่ทั้งหมดวันนี้)
│   │   ├── llm.py              เรียก `claude -p` CLI + นับ token/ค่าใช้จ่าย
│   │   ├── ingest_customers.py STEP1/5 — นำเข้าไฟล์ Excel + คำนวณ feature/persona
│   │   ├── upload.py           ตรวจไฟล์ที่ผู้ใช้อัปโหลด (PII, data contract)
│   │   ├── overrides.py        ของที่คนอนุมัติแล้ว เติมช่องว่างที่ spec ไม่ครอบคลุม
│   │   ├── symbols.py          entity -> สัญลักษณ์ TradingView/Yahoo (สำหรับกราฟ)
│   │   ├── prices.py           ดึง+cache แท่งเทียนจาก Yahoo Finance (หุ้นไทย)
│   │   └── refdata/
│   │       ├── generated.json  สร้างจาก spec โดย build_refdata.py ห้ามแก้มือ
│   │       └── overrides.json  เขียนผ่าน scripts/approve.py เท่านั้น
│   ├── scripts/                 CLI ทั้งหมด (รันด้วย `python -m scripts.xxx`)
│   │   ├── pipeline.py          orchestration หลัก: ingest/news/match/ai/daily/all/status
│   │   ├── ai_enrich.py         CLI เฉพาะงาน AI อ่านข่าว (มี --dry-run, --verbose)
│   │   ├── check_ai_guards.py   ทดสอบด่านตรวจของ AI โดยไม่เรียกโมเดลจริง (เขียนวันนี้)
│   │   ├── build_refdata.py     สร้าง refdata/generated.json จากไฟล์ spec
│   │   ├── approve.py           เขียน overrides.json (ต้องรันมือ)
│   │   ├── propose.py           เสนอ override ให้คนตรวจ (ใช้ anthropic SDK ตรง ไม่ใช่ CLI)
│   │   ├── backfill_fulltext.py ดึงเนื้อหาเต็มของบทความเก่าที่ยังไม่มี
│   │   ├── backfill_toppicks.py เติม entity จากท่อน "หุ้นเด่น" ของบทความเก่า
│   │   ├── redo_toppicks.py     รันซ้ำ top-picks extraction
│   │   └── prune_news.py        ลบข่าวเก่ากว่า RETENTION_DAYS (14 วัน)
│   ├── matchport.db             ฐานข้อมูลจริง (SQLite, WAL)
│   └── backups/                 สำเนาไฟล์ db แบบ manual
└── frontend/
    ├── src/
    │   ├── App.tsx               routing (react-router)
    │   ├── components/Layout.tsx เมนู + แถบเตือน (alerts) + header
    │   ├── pages/                11 หน้า (ดูข้อ 8)
    │   ├── components/           ArticleCard, BriefGroup, Briefing, charts, ui, CandleChart, PriceChart
    │   └── lib/
    │       ├── api.ts             ทุก endpoint + TanStack Query hooks + types (854 บรรทัด)
    │       ├── format.ts          ฟอร์แมตตัวเลข/วันที่/หน่วยเงิน
    │       └── i18n.tsx           dictionary ไทย/อังกฤษ (ไม่มี framework i18n ภายนอก)
    └── dist/                      build output ที่ FastAPI serve ตรง (โปรดักชัน)
```

---

## 4. Data flow แบบละเอียด (จากข่าวดิบ ถึงหน้าจอ RM)

### 4.1 นำเข้าลูกค้า (STEP1 + STEP5) — `ingest_customers.py`

1. อ่านไฟล์ `data/portfolio_masked.xlsx` + `data/txn_masked.xlsx` (ชื่อคอลัมน์ตาม Data
   Contract — ดูตาราง `HOLDING_COLS`/`TXN_COLS`)
2. แปลง `product_code` แต่ละแถวเป็น "รหัสกลาง" ผ่าน `mapping.map_holding()` — สองรอบ:
   - รอบแรก (pass 1) ไม่มี `Universe` เพื่อสร้าง `Universe` ของสิ่งที่ลูกค้าถือขึ้นมาก่อน
   - รอบสอง (pass 2) ใช้ `Universe` นั้นคลี่ underlying ของ Options/DR/TFEX/KIKO ที่ไม่มี
     MIC ติดมาด้วย (เช่น options CBOE ต้องรู้ก่อนว่าลูกค้าถือหุ้นตัวไหนในตลาดไหนจริง ๆ
     ถึงจะสรุปได้ว่า root เดียวกันหมายถึงตัวไหน)
3. บันทึกลง `holdings` / `transactions` แล้วเรียก `build_features()` คำนวณต่อลูกค้าทุกคน
   ใหม่ทั้งหมด (ห้าม cache ข้ามรอบ ตาม R5.4): `asset_mix`, `sector_exposure`,
   `dominant_asset_class`, `persona`, `portfolio_tier`, `trade_frequency`, `watchlist`
   (เทรดใน 90 วันแต่ไม่ถือ), สถานะ TFEX ที่ยังเปิดอยู่ (net Buy(Long) − Sell(Short))
4. เขียนลงตาราง `customers` ทั้งชุด — **"ไฟล์ล่าสุดคือความจริง" (R1.7)**: อัปโหลดไฟล์ใหม่
   จะ `DELETE` ตาราง `customers`/`holdings`/`transactions` แล้วคำนวณใหม่หมด ไม่ merge

**Persona** (STEP5) คือกลุ่มที่ใช้กรองว่าลูกค้าคนนี้ควรได้รับข่าวประเภทไหน — มาจาก
`dominant_asset_class` (สิ่งที่ถือเยอะสุด): `THAI_STOCK`, `US_OFFSHORE`, `FUND_DIY`,
`FUND_ROBO`, `CRYPTO`, `DERIVATIVES`, `BOND`, `DORMANT` (ไม่เทรดเกิน 90 วันและไม่มีการเทรด),
`NO_PORTFOLIO`

### 4.2 ดึงข่าว + แท็ก (STEP2/3) — `news.py`

1. `fetch_page()` เรียก Cafe Invest API (`content/search`) — ใส่ `User-Agent` เอง (GAP-06,
   API ตอบ 403 ถ้าไม่ส่ง) มี rate limit เอง 0.4 วิ/ครั้ง และ retry 3 ครั้ง
2. `tag()` แปลง JSON ดิบหนึ่งชิ้นเป็นแถว `articles` — กำหนด 23 field ตามลำดับความสำคัญของ
   ที่มา entity (R4.33): **field `stock` ของ API ตรง ๆ** (เชื่อถือได้สุด) → รหัสสัญญา TFEX
   ที่ฝังใน summary (GAP-18) → ticker ใน url slug → ticker ใน title → ชื่อ/ตัวย่อในสรุป
   (`build_alias_index()` + `extract_from_text()`)
3. **Brief (morning-brief / evening-brief) ถูกซอยเป็นข้อย่อย** (R3.1-R3.5) — ตัดตามเลขข้อ
   `1. 2. 3. ...` ที่ไล่ลำดับจริงในเนื้อความ (กันเลขกลางประโยคอย่าง "7.2 หมื่นล้าน") แต่ละ
   ข้อย่อยเก็บเป็นแถว `record_type='segment'` แยกจากบทความแม่ (`record_type='article'`)
   ตัวแม่ **ห้ามมี entity รวม** (R3.3) — ถ้ามีจะทำให้ลูกค้าที่ถือ AOT ได้รับ Brief ทั้งฉบับ
   ด้วยเหตุผลของข้อที่พูดถึง DELTA ซึ่งไม่เกี่ยวกัน
4. หลัง `store()` ทุกครั้ง เรียก `verify.regrade()` ให้เกรดคุณภาพ entity ที่สกัดเอง
   (`confirmed` / `auto_verified` / `weak`) แล้ว `backfill_full_text()` ดึงเนื้อหาเต็มของ
   บทความใหม่ (จำกัด 40 ชิ้น/ครั้ง) และ `prune_old()` ลบข่าวเก่ากว่า 14 วัน

**เนื้อหาเต็มมาจากไหน:** search API ส่งมาแค่ย่อหน้าแรก (~500 ตัวอักษร) ตัวเลขที่ RM ใช้คุย
จริง (รายได้ อัตราโต เป้าราคา) อยู่ในเนื้อหาเต็มซึ่งไม่มี API รองรับ (`/content/detail`
ตอบ 400 ทุกพารามิเตอร์ที่ลองแล้ว 19 แบบ) — ระบบจึงดึงหน้าเว็บจริงมาแกะ payload ของ Next.js
(`self.__next_f.push(...)`) เอาเอง (`extract_full_text()` ใน `news.py`) จุดนี้เปราะบางที่สุด
ในระบบเพราะผูกกับโครงสร้างหน้าเว็บของทีมอื่น ถ้าเปลี่ยนจะพังแบบเงียบ — มี fallback ตรวจ
`FULL_TEXT_MIN = 400` ตัวอักษร ถ้าได้น้อยกว่านั้นถือว่าดึงไม่สำเร็จและรายงานเป็น `unmapped`

### 4.3 ตัวตรวจคุณภาพ entity (STEP8) — `verify.py`

Spec เดิมกำหนดว่า entity ระดับ `inferred` (สกัดเอง ไม่ใช่จาก API ตรง) **ต้องมีคนตรวจก่อน**
ถึงจะใช้จับคู่จริงได้ (GAP-21) — เจ้าของงานสั่งยกประตูนั้นออก ทุกบทความเข้าสู่การจับคู่ทันที
ไม่มีคิวรออนุมัติ แต่ยังต้องมีอะไรตรวจซ้ำ ไม่ใช่ตรายาง `grade()` จึงเป็นตัว **ตรวจซ้ำจาก
ข้อมูลจริง** (ไม่ทวนกฎที่ขั้นสกัดกรองไปแล้ว):
- R4.47: ต้องบันทึกไว้ว่า "คำไหน" ทำให้จับ entity ตัวนั้นได้ (ไม่มี token บันทึกไว้ = ตรวจซ้ำไม่ได้)
- R4.43/R4.44: ค้นคำนั้นซ้ำในเนื้อความจริง ๆ (ไม่เชื่อ evidence ที่ขั้นก่อนบันทึกไว้) คำละติน
  ต้องตรงแบบคำเต็ม (word boundary) ไม่ใช่ substring
- R3.38: คำที่ติดเกิน 30% ของคลังบทความ = กว้างเกินจนใช้ไม่ได้ (`corpus_share()`)
- R3.24: ไม่มีลูกค้าถือ = เรื่องปกติ ไม่ตัดเกรด (เป็นข้อมูลประกอบเท่านั้น)

ผลจริงตอนสร้างตัวนี้: จับบั๊กของตัวสกัดได้ 2 ตัว — alias "SpaceX" ไปตรงกับ "SpaceXAI" และ
"Meta" ไปตรงกับ "MetaX" (คนละบริษัท) ซึ่งเคยส่งรายชื่อผิดถึง RM รวม 384 คน (GAP-21)

### 4.4 AI enrichment (AI-01 / AI-02) — `enrich.py` + `llm.py`

**นี่คือส่วนที่เขียนใหม่ทั้งหมดในเซสชันนี้** อธิบายละเอียดในข้อ 5 แยกต่างหาก เพราะเป็นจุดที่
ผู้ใช้ (เจ้าของระบบ) ปรับปรุงคืนนี้

### 4.5 การจับคู่ (STEP6) — `matching.py`

`match_article()` คำนวณคะแนนของบทความหนึ่งชิ้นต่อลูกค้าหนึ่งคน จากระดับความเกี่ยวข้อง 6 ชั้น
(เรียงน้ำหนักจากมากไปน้อย):

| ระดับ | ความหมาย | น้ำหนักฐาน |
|---|---|---|
| `L1_HOLD` | ถือ entity ที่ข่าวพูดถึงตรง ๆ | 100 |
| `L2_WATCH` | เคยเทรดใน 90 วัน ตอนนี้ไม่ถือ | 60 |
| `L3_SECTOR` | ถือหุ้นกลุ่มเดียวกัน (หุ้นไทยเท่านั้น) | 40 |
| `L4_RELATED` | ถือหุ้นที่ถูกกล่าวถึงร่วมกันบ่อย (co-mention ≥3 หรือ seed 6 กลุ่ม) | 30 |
| `L5_ASSET` | ถือสินทรัพย์ประเภทเดียวกัน (ข่าวภาพรวมไม่เจาะหุ้น) | 20 |
| `L6_MACRO` | พอร์ตไวต่อประเด็นมหภาคเดียวกัน (ถ่วงตาม `MACRO_SENSITIVITY`) | 10 |

สูตรคะแนนสุดท้าย (แสดงบนหน้า `/spec` ด้วย):

```
score = base_weight(ระดับสูงสุดที่เข้า)
      × (1 + log_scale(มูลค่าที่ถือ))     R6.8 — log กันรายใหญ่กลบทุกคน อิ่มตัวที่ 20 ลบ.
      × importance / 3                    R6.9
      × urgency_factor                    R6.10 — now=1.5, this_week=1.0, low=0.7
      × recency_factor                    R6.11 — เพิ่งเทรดตัวนั้น (≤7 วัน = ×1.3, ≤30 = ×1.1)
      × (1 + 0.1 × (n_hits - 1))          โบนัสถ้าเข้าหลายระดับพร้อมกัน
```

ต่ำกว่า `SCORE_THRESHOLD = 50.0` (R6.14, ค่าคงที่แก้ได้ที่ `tables.py` จุดเดียว) ไม่แสดง แต่
ไม่จำกัดจำนวนคนที่ผ่านเกณฑ์ (R6.15, ไม่ cap แค่เรียงลำดับ)

**mode** ของบทความ (`realtime` สำหรับข่าวรายตัว vs `digest` สำหรับสรุป/บทวิเคราะห์) จำกัดว่า
จับคู่ได้ถึงระดับไหน (R6.12/R6.13): `realtime` ถึงแค่ L2, `digest` ถึง L4 นี่คือที่มาของ
`GAP-20` (L4 ถูกออกแบบมาแทน L3 สำหรับหุ้นนอกที่ไม่มี sector แต่ข่าวหุ้นนอกทั้งหมดเป็น
`company_news` = realtime ซึ่งจับได้แค่ L1-L2 เท่านั้น — L4 จึงไม่เคยทำงานจริงกับหุ้นนอก
เป็นช่องว่างที่ยังไม่ได้แก้ ต้องให้ business ตัดสินใจ)

`run_for_articles()` เป็นจุดเรียกหลัก — วนทุกบทความที่ยังไม่จับคู่ (หรือทั้งหมดถ้าไม่ระบุ
`only_unmatched`), ลบ `matches` เก่าของบทความนั้นแล้วเขียนใหม่ทั้งชุด (ไม่ merge)

### 4.6 ทิศทางข่าว + ประเด็นคุย (GAP-22) — `briefing.py`

Spec เดิม (STEP7 หลักการข้อ 6) ห้ามบอกว่าข่าวดีหรือร้าย และ STEP3 เคยตัด sentiment ทิ้งเพราะ
ทดสอบ 300 บทความได้แค่ 42% แม่นยำ (ข่าวประธานลาออกถูกติดว่าบวก) — เจ้าของงานสั่งให้ใส่กลับ
`briefing.analyse()` จึงไม่เดาอารมณ์จากถ้อยคำ แต่ยกสิ่งที่ **มีแหล่งอ้างอิงได้จริง** ขึ้นมา
3 ชั้น เรียงน้ำหนักจากแน่นสุด:

1. **คำแนะนำที่ INVX ประกาศเอง** — พาดหัวเขียน "แนะนำซื้อ/แนะนำขาย/LONG/SHORT" ตรง ๆ หรือ
   rating + ราคาเป้าหมายจาก Thai Stock Coverage List (Outperform/Underperform)
2. **ผลเทียบกับที่คาด** — "ดีกว่าคาด/ต่ำกว่าคาด/ตามคาด" เป็นการรายงานผล ไม่ใช่ความเห็น
   รวมถึงประโยคที่ผู้เขียนระบุทิศทางไว้เอง ("เป็นบวกต่อ...", "เป็นลบต่อ...")
3. **โทนของพาดหัว** — คำเช่น "ทำสถิติสูงสุด" / "ปรับลดเป้า" ถือเป็นการอ่านถ้อยคำ ใช้เป็น
   ทางเลือกสุดท้ายเท่านั้น (แสดงเฉพาะเมื่อไม่มีชั้น 1/2 เลย) และบังคับแสดงวลีที่ทำให้ติดเสมอ
   เพื่อให้ RM ค้านได้ในหนึ่งสายตา

บทความที่พูดสองทาง (เช่นดอกเบี้ยขึ้นดีต่อธนาคารแต่ร้ายต่อกลุ่มอื่น) **ไม่ถูกยุบเป็นค่าเดียว**
— ขึ้นเป็น `two_sided` หรือ `position_dependent` แทน คนละเรื่องกับ "ขัดกันเองเพราะกฎอ่านผิด"
ซึ่งจะตอบ `unknown` เพราะไม่มีหลักฐานพอ (ตรงนี้เป็นจุดที่ AI-01 เข้ามาเสริมได้ — ดูข้อ 5)

`talking_points()` ประกอบประเด็นคุยจากข้อเท็จจริงที่ระบบมีอยู่แล้วทั้งหมด (สัญญาณ, ประเด็น
สองทาง, ระดับที่เข้าเกณฑ์, ป้ายเตือนชนิดสินทรัพย์อย่าง DR/bond/TFEX, ความสดของข้อมูล) และ
ปิดท้ายด้วยข้อความ disclaimer เสมอว่านี่ไม่ใช่คำแนะนำการลงทุน — **ไม่เรียก LLM ในโมดูลนี้**
เพราะข้อความที่ RM เอาไปคุยกับลูกค้าต้องได้ผลเดิมทุกครั้งกับ input เดิม และต้องชี้แหล่งที่มา
ได้ทุกบรรทัด

---

## 5. ระบบ AI enrichment (AI-01 / AI-02) — เขียนใหม่ทั้งหมดในเซสชันนี้

### 5.1 ปัญหาที่แก้

ระบบเดิม (`enrich.py` ก่อนแก้) ถามโมเดลแค่ "หุ้นไหนบทความแนะนำ (picks) / มองลบ (negative)"
ทำให้โมเดลต้อง **คัดออกเองอย่างเงียบ ๆ** ว่าตัวไหนไม่นับ ไม่มีทางรู้ว่าทิ้งเพราะอะไร และไม่มี
ด่านตรวจกันโมเดลแปลงชื่อบริษัทเป็นตัวย่อเอง หรือกันตัวย่อชนกันข้ามตลาด/ข้ามประเทศ

ปัญหาที่เจอจริงจากการทดสอบกับข่าวจริงในคลัง:
- บทความ "IBIDEN ผู้ผลิตแผ่นฐานรองชิป AI" เอ่ยถึง NVIDIA/TSMC/AMD ประกอบ (เล่าบริบท) แต่
  ไม่ได้วิเคราะห์ตัวเหล่านั้น — โมเดลเดิมจะเสี่ยงนับเป็น "หุ้นเด่น"
- ตัวย่อ `TEL` = Tokyo Electron (ญี่ปุ่น) แต่ในพอร์ตลูกค้ามี `TEL:xnys` = TE Connectivity
  (สหรัฐ) — ถ้าไม่เช็กตลาด จะส่งชื่อ TE Connectivity ให้ลูกค้าที่ไม่เกี่ยวข้องกับ Tokyo
  Electron เลย
- ข่าวหุ้นนอกล้วน ๆ ที่บังเอิญมีตัวย่อสะกดเหมือนหุ้นไทย (`SCB`, `DELTA`) เสี่ยงส่งรายชื่อ
  ลูกค้าหุ้นไทยผิดกลุ่ม

### 5.2 Prompt v2 — `stocks[]` พร้อม `role`

แทน `picks`/`negative` ด้วยรายการเดียวที่บังคับให้ AI **ใส่ทุกตัวที่เอ่ยถึง** พร้อมติด role:

| role | ความหมาย | โค้ดรับหรือไม่ |
|---|---|---|
| `recommend` | บทความแนะนำ/มองบวกตรง ๆ | รับ — ทับ evidence เดิมได้ |
| `avoid` | บทความมองลบ/ให้เลี่ยง | รับ — ทับ evidence เดิมได้ |
| `analyze` | วิเคราะห์ตัวนั้นเป็นเรื่องหลัก ไม่ชี้ซื้อ/เลี่ยง | รับ — ไม่ทับของเดิม (`setdefault`) |
| `mention_only` | ยกตัวอย่าง เล่าประวัติ เอ่ยประกอบ | **ไม่รับ** — บันทึกไว้ว่าทำไมไม่นับ |

โมเดลไม่แน่ใจระหว่าง `analyze` กับ `mention_only` ถูกบังคับให้ตอบ `mention_only` เสมอ (เอียง
ไปทางระวังไว้ก่อน) พร้อมกันนั้นบังคับให้ตอบ `market` (`TH`/`US`/`JP`/`HK`/`CN`/`KR`/`TW`/
"ต่างประเทศ") และรับได้ทั้งตัวย่อ (`symbol`) และชื่อบริษัท (`name`) เพราะบทความไทยหลายชิ้น
เขียนแต่ "Nvidia" ไม่มีตัวย่อเลย

### 5.3 ด่านตรวจ 4 ชั้น (`judge_stocks()` ใน `enrich.py`)

1. **คำต้องอ่านได้จากบทความจริง** (`_seen_in()`) — ตัวย่อเทียบตรงตัวพิมพ์แบบคำเต็ม
   (word-boundary) ชื่อบริษัทที่อยู่ในพจนานุกรมที่คนคัดแล้วเทียบแบบไม่สนตัวพิมพ์ได้ กันโมเดล
   นึกตัวย่อขึ้นเองจากชื่อบริษัท
2. **quote ต้องมีอยู่จริงในบทความ** (`llm.verify_quote()`) — เทียบแบบยุบช่องว่าง/ปรับ
   เครื่องหมาย ยอมให้ตรงแค่ 24 ตัวอักษรแรกได้ (กันกรณีโมเดลยกมาแล้วต่อท้ายข้อความของประโยค
   ถัดไป)
3. **quote ต้องเอ่ยถึงหุ้นตัวนั้นจริง** — กันโมเดลยกประโยคของหุ้นตัวอื่นมาแปะผิดตัว
4. **ตลาดต้องไม่ขัดกัน** (`_resolve()` + `_model_market()`) — ใช้ `market` ที่โมเดลตอบมา
   จำกัด MIC ที่ยอมรับผ่าน `BLOOMBERG_COUNTRY` (ตารางเดียวกับที่ `mapping.py` ใช้แปลงรหัส
   ฝั่งลูกค้า) กันตัวย่อชนข้ามประเทศ/ข้ามตลาด และกันบทความหุ้นนอกล้วนแปลงเป็นรหัสหุ้นไทย

การแปลงเป็น entity (`_resolve()`) ยังคงลำดับความน่าเชื่อถือเดิม: alias index (Coverage List
+ พจนานุกรมชื่อบริษัท ที่คนคัดแล้ว) ก่อน แล้วค่อยของที่ "ลูกค้าถืออยู่จริง" แม้ตัวย่อจะสั้น
แค่ 2 ตัวอักษร (เช่น `TU`, `AP`) ซึ่งปกติ R4.44 จะกันไว้เพราะกลัวกำกวมเวลากวาดข้อความทั้ง
บทความ — แต่ที่นี่ต่างกันเพราะมีประโยคยกมาเป็นหลักฐานและผ่านด่าน 1+3 มาแล้ว

### 5.4 AI-02 — "ไม่ตีความ" ต้องบอกได้ว่าเพราะอะไร

จุดที่ผู้ใช้กังวลตรง ๆ: กลัวข่าวที่แค่เล่าเรื่องบริษัท (company history) ถูกตีความเป็นการ
แนะนำ ตอนนี้เมื่อ AI อ่านแล้วสรุปทิศทางไม่ได้ (`overall == "unknown"`) จะบังคับให้ตอบ
`no_call_reason` จากรายการปิด (ไม่ใช่ข้อความอิสระ):

- `history_only` — เล่าประวัติ/อธิบายตัวบริษัท
- `data_only` — รายงานตัวเลข/เหตุการณ์เฉย ๆ ไม่ได้สรุปทิศทาง
- `no_view` — ไม่ได้บอกทิศทางไว้
- `not_about_stock` — ไม่ได้พูดถึงสินทรัพย์รายตัว
- `conflicting` — พูดสองด้านโดยไม่สรุป
- `other` — อ่านแล้วไม่พบประโยคบอกทิศทางตรง ๆ

บวกเหตุผลที่ **โค้ด** เป็นคนตัดสิน (ไม่ใช่โมเดล): `quote_failed` (โมเดลบอกทิศทางมาแต่ประโยค
ที่ยกมาไม่มีในบทความ) และ `bad_answer`/`no_text` เก็บลง 3 คอลัมน์ใหม่ใน `articles`:
`ai_reason`, `ai_reason_th` (ประโยคที่หน้าจอแสดงตรง ๆ), `ai_mentions` (JSON — **ทุก**ตัวย่อ
ที่โมเดลเห็น พร้อมผลตรวจว่ารับหรือทิ้งเพราะอะไร ไม่ใช่แค่ตัวที่รับ)

`briefing.analyse()` ถูกแก้ให้ดึง `ai_reason_th` มาใส่ใน `no_call_th` ของผลลัพธ์เมื่อ
`overall == "unknown"` — แปลว่าฝั่ง backend พร้อมส่งเหตุผลนี้ให้หน้าเว็บแล้ว **แต่ฝั่ง
frontend (`Briefing.tsx`, `api.ts` type `Briefing`) ยังไม่ได้ต่อรับ field `no_call_th` /
`no_call_code` นี้ — เป็นงานค้างที่ต้องทำต่อถ้าต้องการให้ RM เห็นเหตุผลบนหน้าจอจริง**

### 5.5 กฎเองก็อาจ "จับเกิน" ได้เหมือนกัน — การ demote

ระหว่างทดสอบพบว่า **กฎเดิม** (ไม่ใช่แค่ AI) ก็เคยติด entity ผิดจากการเจอชื่อในสรุปตรง ๆ
(R4.42 "พบชื่อในสรุปบทความ") เช่นบทความเรื่อง Tongfu Microelectronics ที่แค่เอ่ย TSMC/AMD
ประกอบ กลับติด entity `TSM:xnys`/`AMD:xnas` เข้าไปแล้ว ทำให้ลูกค้าที่ถือ AMD ได้รับข่าวที่
ไม่เกี่ยวกับ AMD จริง ๆ

`judge()` จึงเทียบ entity ที่ AI เห็นว่าเป็น `mention_only` กับ entity ที่กฎติดไว้แล้ว
(`have`) ถ้าตรงกัน จะ:
- ค่าเริ่มต้น (`demote=False`): **ติดป้ายเตือนใน evidence** (`ai_mention_only: true` +
  ต่อท้ายข้อความอธิบาย) แต่ **ไม่ถอนออก** — เพราะการถอนรายชื่อที่ RM เคยเห็นควรเป็นการ
  ตัดสินใจของคน ไม่ใช่ผลข้างเคียงอัตโนมัติของการอ่านข่าว
- ถ้าสั่ง `--demote-mentions` (CLI) หรือ `demote=True` (ฟังก์ชัน): ถอน entity นั้นออกจริง
  พร้อมล้าง `matched_at` ให้จับคู่ใหม่

### 5.6 ต้นทุน token — วัดจริง ไม่ใช่ประมาณ

`claude -p` เวอร์ชันไม่ตั้งค่าอะไรแบก system prompt ของตัวช่วยเขียนโค้ด + คำอธิบายเครื่องมือ
ทั้งชุดไปด้วยทุกครั้ง (วัดได้ 32,485 token/ครั้งสำหรับ prompt สั้น ๆ) `llm.py` จึงตั้ง
`SYSTEM_PROMPT` สั้น ๆ เฉพาะงานนี้ ("อ่านบทความไทยแล้วตอบ JSON") และปิดเครื่องมือทั้งหมดด้วย
`--disallowed-tools` (ไม่ต้องใช้ Bash/Read/Write ฯลฯ เลยสำหรับงานนี้) ลดเหลือ ~8,300 token
(ถูกลง 4 เท่า) วัดจริงกับข่าว 1 ชิ้น (segment ยาว ~2,900 ตัวอักษร): **~19,400 token
(input+cache) รวม output ≈ $0.05/ชิ้น**

`llm.usage()` เก็บยอดสะสม (calls, input, cache_write, cache_read, output, cost_usd) เป็น
global ที่ thread-safe (`threading.Lock`) เพื่อตอบคำถาม "รันวันนี้กิน token ไปเท่าไหร่" ได้
ทันทีหลังจบงาน — `scripts/ai_enrich.py` และ `scripts/pipeline.py` พิมพ์สรุปนี้ท้ายทุกครั้ง

**ข้อควรระวังสำคัญ:** `claude -p` ที่ `enrich.py` เรียกใช้ **login เดียวกับที่ผู้ใช้คุยกับ
Claude Code อยู่ตอนนี้** (ไม่มี `ANTHROPIC_API_KEY` แยก) ดังนั้นถ้ารัน AI enrich จำนวนมาก
จะไปแย่ง weekly quota เดียวกันกับ session แชท — เป็นเหตุผลที่ยังไม่ต่อ AI เข้ากับปุ่ม "ดึง
ข่าว" บนหน้าเว็บโดยอัตโนมัติ (ดูข้อ 5.7)

### 5.6.1 สรุปขั้นตอนทั้งหมดของข่าว 1 ชิ้น พร้อมต้นทุนจริง

ตารางนี้คือคำตอบเวลาถูกถามว่า "ดึงข่าว 1 ชิ้นเสียอะไรไปบ้าง" — แยกให้เห็นชัดว่าขั้นไหน
เกิดขึ้น**อัตโนมัติทันทีตอนกดปุ่มดึงข่าว** (ฟรี ไม่มี AI) กับขั้นไหนที่**ต้องสั่งแยก**
(เสีย token จริง) ตัวเลขต้นทุน/เวลาวัดจริงจากการรันคืนนี้ ไม่ใช่ประมาณ

| # | ขั้นตอน | ทำอะไร | ต้นทุน token | เวลา | อัตโนมัติ? |
|---|---|---|---|---|---|
| 1 | เรียก API | ดึง JSON ดิบจาก Cafe Invest | 0 | ~0.4 วิ/หน้า × จำนวนหน้า (`--pages 4` ค่าเริ่มต้น) | ใช่ |
| 2 | แท็ก entity/sector/macro | กฎคำ 5 ชั้น ไม่มี LLM | 0 | เสี้ยววินาที/ชิ้น (ตัวประมวลผลเอง เร็ว) | ใช่ |
| 3 | ให้เกรดคุณภาพ | ตรวจซ้ำคำที่อ้างว่าเจอ | 0 | เสี้ยววินาที/ชิ้น | ใช่ |
| 4 | ดึงเนื้อหาเต็ม | โหลดหน้าเว็บจริง แกะข้อความ | 0 | ~0.4 วิ/ชิ้น × สูงสุด 40 ชิ้น/รอบ (ตัวที่กินเวลาจริงส่วนใหญ่) | ใช่ |
| 5 | จับคู่ลูกค้า (L1-L6) | คำนวณคะแนน เขียนลง `matches` | 0 | เสี้ยววินาที/ชิ้น — ทั้งฐาน 350+ ชิ้นวัดได้จริง 4.6-5.4 วิ | ใช่ |
| 6 | **AI อ่านเนื้อหาเต็ม** | Claude อ่าน `full_text` เสนอหุ้น+ทิศทาง | **~19,400 token** (input+cache รวม) | **~15-25 วิ/ชิ้น** | **ไม่ — ต้องสั่งแยก** |
| 7 | ตรวจข้อเสนอ AI (4 ด่าน) | โค้ดตรวจซ้ำ ไม่เรียกโมเดลเพิ่ม | 0 | เสี้ยววินาที | ใช่ (ถ้าทำขั้น 6) |

**ต้นทุนขั้น 6 แยกละเอียด** (วัดจริงจากบทความ Tongfu Microelectronics, เนื้อหา ~2,900 ตัวอักษร):

| รายการ | ค่า |
|---|---|
| input token | 2 |
| cache_write (เขียน cache ใหม่) | 6,644 |
| cache_read (อ่านจาก cache เดิม) | 12,193 |
| output token | 556 |
| **รวม** | **~19,395 token** |
| **ค่าใช้จ่าย** | **$0.052** |

ตัวเลขนี้คือ**หลังปรับให้ประหยัดแล้ว** (ตัด system prompt ของตัวช่วยเขียนโค้ดออก ปิดเครื่องมือที่
ไม่ใช้ทั้งหมด) ค่าเริ่มต้นของ `claude -p` แบบไม่ตั้งค่าอะไรอยู่ที่ ~32,500 token/ครั้งก่อนเข้า
เนื้อหาจริงด้วยซ้ำ — ลดมาได้ 4 เท่า

**สรุปให้จำง่าย — แก้ตัวเลขให้ตรงกับที่วัดจริง:** ขั้น 1-5 ไม่เสีย token เลย (ไม่มี LLM)
แต่**เวลารวมทั้งคลิกปุ่ม "ดึงข่าว" ไม่ใช่ <1 วินาที** ตัวเลข <1 วิ ใช้ได้แค่ระดับ "ประมวลผล
ต่อบทความ 1 ชิ้นถ้าตัดเรื่อง network ออก" (ขั้น 2/3/5 เป็น regex/dict lookup ล้วน เร็วจริง)

ตัวที่กินเวลาจริงคือ **network I/O ที่ rate-limit ไว้เอง** (ขั้น 1 เรียก API หลายหน้า + ขั้น 4
ดึงเนื้อหาเต็มทีละหน้าเว็บ จำกัด 40 ชิ้น/รอบ แต่ละชิ้น rate-limit 0.4 วิ) วัดจริงจากการรัน
`pipeline news --pages 4` คืนนี้: **ทั้งคลิกใช้เวลา 83.7 วินาที** ส่วนขั้นจับคู่ (5) เพียวไม่มี
network วัดแยกได้ **4.6-5.4 วินาทีสำหรับทั้งฐาน 350+ บทความพร้อมกัน** (เร็วจริงตามที่อ้าง)

ที่ต้องรอเพิ่ม 15-25 วินาที/ชิ้นและเสีย token คือเฉพาะตอนสั่ง "ให้ AI อ่าน" เพิ่มเติมเท่านั้น
(ขั้น 6-7) ซึ่งเป็นคนละปุ่ม/คนละคำสั่งกับการดึงข่าวธรรมดา (ดูข้อ 10.1 สำหรับแผนรวมสองปุ่มนี้)

### 5.7 จุดที่ยังไม่ได้ต่อ — ต้องตัดสินใจ

ผู้ใช้ถามระหว่างเซสชันนี้ว่า "อยากให้ AI รันทุกครั้งที่ดึงข่าว" มีทางเลือก 2 ทาง ที่ยังไม่ได้
ตัดสินใจสุดท้าย:

1. **CLI เท่านั้น (ทำเสร็จแล้ว, แนะนำ)** — `python -m scripts.pipeline daily` รันเอง
   ตอนเช้า/ตั้ง cron ทำ 3 ขั้นในคำสั่งเดียว: ดึงข่าว → ให้ AI อ่านของวันนั้น → จับคู่ใหม่
   ปุ่ม "ดึงข่าว" บนหน้าเว็บ **ยังไม่เรียก AI** กดกี่ครั้งก็ไม่กิน quota เพิ่ม
2. **ต่อเข้าปุ่มเว็บ (ยังไม่ทำ)** — แก้ `POST /api/ingest/news` (`main.py:1260`) ให้เรียก
   `enrich.enrich_batch()` ต่อท้ายทุกครั้งที่กดปุ่ม "ดึงข่าว" — สะดวกกว่าแต่ทุกคลิกจะกิน
   Claude quota โดยอัตโนมัติ ไม่ผ่านการยืนยัน

**สถานะจริงตอนนี้คือทางเลือกที่ 1** `run_ai()` ใน `scripts/pipeline.py` ถูกต่อเข้ากับคำสั่ง
`ai`/`daily`/`all --ai` เรียบร้อย ทดสอบแล้วว่ารันจริงได้ (news → ai → match ในคำสั่งเดียว)
ถ้าจะพิชวันนี้ว่า "AI อ่านข่าวอัตโนมัติทุกครั้งที่ดึง" คำพูดนี้ **ถูกต้องสำหรับ CLI/cron**
แต่ **ไม่ถูกต้องสำหรับปุ่มบนหน้าเว็บ** — ควรระบุให้ชัดตอนพูดกับทีม/ลูกค้าว่าหมายถึงทางไหน

---

## 6. Database schema (SQLite, `backend/matchport.db`)

| ตาราง | ใช้ทำอะไร | คีย์สำคัญ |
|---|---|---|
| `customers` | ลูกค้า 1 แถว/คน พร้อม feature ที่คำนวณแล้ว (persona, portfolio_tier, ...) และ JSON blob (`holdings`, `watchlist`, `asset_mix`, `sector_exposure`) | `customer_key` (PK) |
| `holdings` | รายการถือครองดิบต่อแถว หลังแปลงเป็นรหัสกลางแล้ว | index บน `customer_key`, `entity` |
| `transactions` | ธุรกรรมย้อนหลัง 6 เดือน ใช้คำนวณ watchlist/ความถี่เทรด | index บน `customer_key`, `entity`, `txn_date` |
| `articles` | บทความ/ข้อย่อยของ Brief ทุกชิ้น (`record_type` = article/segment) พร้อมผลแท็กทั้งหมดและคอลัมน์ AI (`ai_direction`, `ai_reason`, `ai_mentions`, ...) | `article_id` (PK, ข้อย่อยคือ `<parent>#<n>`) |
| `matches` | ผลจับคู่ (บทความ, ลูกค้า) หนึ่งแถวต่อคู่ พร้อมคะแนน/หลักฐาน | unique (`article_id`, `customer_key`) |
| `unmapped` | ทุกอย่างที่ระบบจัดการไม่ได้ — "ไม่มั่นใจ ไม่เดา" (ดูข้อ 7) | unique (`bucket`, `raw`, `rule`) |
| `entity_pairs` | ความสัมพันธ์หุ้นที่เรียนเองจาก co-mention (สำหรับ L4) | PK (`a`, `b`) |
| `settings` | key-value เก็บ timestamp ต่าง ๆ (`data_as_of`, `matched_at`, ฯลฯ) | `k` (PK) |
| `price_cache` | แท่งเทียนจาก Yahoo, cache 30 นาที | `k` = `symbol\|range\|version` |
| `ingest_log` | ไม่ได้ใช้งานจริงในโค้ดปัจจุบัน (เตรียมไว้) | — |

Migration ทำแบบ additive เท่านั้น (`db.MIGRATIONS` ใน `db.py`) — `ALTER TABLE ADD COLUMN`
รันทุกครั้งที่ `db.init()` ถูกเรียก (ตอน startup ของ FastAPI และทุก CLI) ไม่มี down-migration

---

## 7. หลักการ "ไม่มั่นใจ ไม่เดา" ในทางปฏิบัติ

ทุกจุดในระบบที่แปลง/สกัด/จับคู่อะไรไม่ได้ เรียก `db.report_unmapped(con, bucket, raw, rule,
reason, ref, now)` แทนที่จะทิ้งเงียบ — ข้อมูลไปโผล่ที่หน้า `/reports` แท็บ "unmapped" จัด
ลำดับตาม **ผลกระทบจริง** (คำนวณจากพอร์ต: มีคนถือกี่คน คิดเป็นเงินเท่าไร) ไม่ใช่จำนวนครั้งที่
เจอ — เพราะรหัสที่เจอ 84 ครั้งแต่มีคนถือ 2 คน ไม่สำคัญเท่ารหัสที่เจอ 3 ครั้งแต่มีคนถือ 200 คน

`bucket` ที่ใช้จริงในโค้ด: `subcategory` (หมวดข่าวใหม่ที่ไม่รู้จัก — ร้ายแรงสุด ทิ้งทั้งหมวด),
`news_entity`, `holding_code`, `asset_class`, `txn_type`, `customer_key`, `segment`,
`full_text`, `tfex`, `ai` (ข้อเสนอของ AI ที่ไม่ผ่านด่านตรวจ) — แต่ละ bucket มีระดับความรุนแรง
คงที่ (`high`/`medium`/`low`) กำหนดไว้ใน `tables.UNMAPPED_SEVERITY`

`main.py` ยังมี `_alerts()` ที่ยกเรื่องเร่งด่วนขึ้นเป็นแถบเตือนบนหัวทุกหน้า (หมวดใหม่ที่ไม่
รู้จัก, ของใหม่ที่เพิ่งโผล่ใน 7 วัน, ข้อมูลลูกค้าเก่าเกิน 7 วัน) — ก่อนหน้านี้ตาราง
`unmapped` มีอยู่แต่ไม่มีอะไรส่งเสียง หมวดข่าวใหม่จึงหายไปเงียบ ๆ ได้เป็นเดือน

---

## 8. Frontend — ทุกหน้า

Stack: React 18 + TypeScript + Vite + `@tanstack/react-query` (จัดการ fetch/cache) +
`react-router-dom` (routing) + Tailwind (utility class ล้วน ไม่มี component library ภายนอก)
ไม่มี state management library แยก (ใช้ query cache ของ TanStack Query เป็น source of truth)

รองรับสองภาษา (ไทย/อังกฤษ) ผ่าน dictionary เดียวใน `lib/i18n.tsx` (ไม่ใช้ i18next หรือ
framework อื่น) — ข้อความสำหรับ RM **ไม่มีเลขกฎหรือชื่อ field ภายใน** รายละเอียดเชิงเทคนิค
อยู่เฉพาะหน้า `/spec` และ `/reports` เท่านั้น

### 8.1 `/` — Stock (หน้าแรก, "หาจากหุ้น")

หน้าเดียวที่ทำสองหน้าที่ (ผ่าน `useParams` ว่ามี `:entity` หรือไม่):
- **ไม่มี entity** → แสดง `Highlights()` — สินทรัพย์ที่ควรสนใจวันนี้ ประกอบจาก 5 กลุ่ม:
  ข่าววันนี้+มีคนถือ (เรียงตามคนเข้าเกณฑ์/มูลค่า), ข่าวลบ+พอร์ตขาดทุนรวม ("ควรโทรก่อน"),
  ประเด็นที่พูดถึงซ้ำในสัปดาห์, ข่าวมาแต่ยังไม่มีคนถือ (โอกาสเสนอใหม่), ถือเยอะสุดในพอร์ตรวม
  ทุกแถวต้องมีเหตุผลกำกับ (`why_th`/`phrase`) ไม่ใช่คะแนนลอย ๆ — ตัวกรอง `rm` เปลี่ยนมุมมอง
  จาก "ทั้งบริษัท" เป็น "เฉพาะลูกค้าของคนนี้" (ค่าเริ่มต้นคือทั้งบริษัท เพราะคนดูภาพรวมไม่ได้
  ดูแลพอร์ตเอง — สอดคล้องกับผู้ใช้กลุ่มที่ 2)
- **มี entity** → ค้นหุ้น/กองทุน/คริปโตแล้วดู: ใครถือเยอะสุด (แยกตาม RM), ข่าวช่วงนี้ว่าอะไร
  (พร้อมทิศทางรายชิ้น), กราฟราคา (`PriceChart`), rating จาก INVX Research, ใครเคยเทรดแต่
  ไม่ถือแล้ว (`watchers`)

### 8.2 `/today` — Today (ข่าววันนี้)

หน้าที่คนเปิดทุกเช้าตาม STEP7 — จัดกลุ่มข่าวเป็น 3 ช่วงตามเวลาจริง (ไม่ใช่การ์ดที่สลับกัน
ได้): `morning` (ก่อนเที่ยง), `intraday` (ข่าวรายตัวที่เพิ่งออก, `mode=realtime`), `evening`
(สรุปวัน) มีปุ่ม "ดึงข่าว" (เรียก `POST /api/ingest/news`) และปุ่ม "ให้ AI อ่านข่าว N ชิ้น"
(เรียก `POST /api/ai/enrich`, ซ่อนถ้า `ai.available === false`) อยู่ติดกันเพราะเป็นขั้นถัด
ของงานเดียวกัน มีปฏิทินเลื่อนดูวันก่อนหน้าได้ + แถบปริมาณข่าวรายวัน (`DayColumns`)

`groupBrief()` ยุบข้อย่อยของ Brief ที่มาจากฉบับเดียวกันให้เป็นการ์ดเดียว (`BriefGroup`)
แทนที่จะแสดงทีละข้อ — เหตุผล: ข้อย่อยของ brief 213 ข้อในคลัง จับคู่ได้จริงแค่ 43 ข้อ (20%)
ถ้าแสดงแยกทุกข้อจะดันข่าวรายตัวที่มีคนรอโทรตกไปข้างล่าง

### 8.3 `/news` — News (ข่าวทั้งหมด)

รายการค้นหา/กรองบทความทั้งหมด (คำค้น, mode, subcategory ยอดนิยม 12 อันดับ, เฉพาะข้อย่อย,
เรียงตามล่าสุด/จำนวนคนเข้าเกณฑ์/importance) ใช้ `ArticleCard` แสดงแต่ละแถว

### 8.4 `/news/:id` — Article (รายละเอียดข่าว + รายชื่อที่ควรติดต่อ)

หน้าที่ซับซ้อนที่สุดหน้าหนึ่ง — แสดงเนื้อบทความ, entity/sector/macro ที่แท็กได้, แถบเกรด
คุณภาพ (`GradeBar`, แสดงเฉพาะเมื่อเกรด `weak`), `<Briefing>` (ทิศทาง+ประเด็นคุย), รายการ
ข้อย่อยถ้าเป็น Brief แม่, และตารางรายชื่อลูกค้าที่ควรติดต่อ — กรองได้ตามระดับ/RM/persona
กดแถวขยายดู "หลักฐาน" เต็ม (ทุกปัจจัยที่คูณกันได้คะแนนสุดท้าย) และ "ทำไมอยู่อันดับนี้"
ปุ่ม "คัดลอกรายชื่อ" copy เป็น tab-separated text ปุ่มย้อนกลับจำหน้าที่มาไว้ (state
`{from: ...}`) เพื่อไม่ให้เด้งไปหน้า "ข่าวทั้งหมด" เสมอเหมือนเดิม

`noMatchReason()` อธิบายให้ RM เข้าใจว่าทำไมข่าวชิ้นนี้ไม่มีใครเข้าเกณฑ์ (แยกกรณี: เป็น
ประเด็นภาพรวมล้วน ๆ ที่ realtime จับได้แค่ L1-L2 vs ไม่มีใครได้คะแนนถึงเกณฑ์) — ไม่ปล่อย
ให้เดาว่าระบบพัง

### 8.5 `/rm` — RmQueue (รายชื่อที่ต้องโทร)

หน้าที่ RM ใช้จริงในการทำงาน ตอบ 4 คำถามตามลำดับที่ถามจริง ผ่าน 3 มุมมอง (segmented
control): **ตามลูกค้า** (`ByCustomer`, ค่าเริ่มต้น เรียงตาม "เงินที่ข่าวแตะ" ไม่ใช่คะแนน —
ลูกค้าคะแนน 51 ที่ถือของตัวนั้น 15 ลบ. คุ้มกว่าคะแนน 540 ที่ถือ 3 แสน), **ตามข่าว**
(`ByNews`, ข่าวชิ้นไหนแตะเงินลูกค้ามากสุด พร้อมลูกค้ารายใหญ่สุดของชิ้นนั้น), **ตามหุ้น**
(`ByEntity`, ข่าววันนี้ทำให้ตัวไหนต้องโทรคุย — เป็นการ์ดไม่ใช่ตาราง เพราะข้อมูลต่อหุ้นมี
หลายชนิดต้องอ่านพร้อมกัน) มูลค่ารวมแบบไม่นับซ้ำต่อหุ้น (ยุบด้วย `MAX` ก่อนรวมข้ามหุ้น)
กันกรณีข่าวถึงหุ้นเดียวกันหลายชิ้นในวันเดียวทำให้ยอดบวมเกินมูลค่าพอร์ตจริง

### 8.6 `/customers`, `/customers/:key` — Customers, Customer

รายชื่อลูกค้าทั้งหมด (กรองด้วย persona/RM/tier/คำค้น) และหน้ารายละเอียดต่อคน (สัดส่วน
สินทรัพย์, กลุ่มอุตสาหกรรมที่ถือ, ข่าวที่ระบบเสนอให้ติดต่อ, holdings/transactions ดิบ)
แสดงเป็นรหัสลูกค้าเท่านั้น ไม่มีชื่อจริง/เลขบัญชี (PII ถูกกรองตั้งแต่ตอนอัปโหลด — ดู 8.10)

### 8.7 `/reports` — Reports (ตรวจสอบข้อมูล)

4 แท็บ: **unmapped** (ของที่จัดการไม่ได้ เรียงตามผลกระทบ), **verification** (เกรดคุณภาพ
entity ต่อบทความ พร้อมรายการตรวจทีละข้อ), **coverage** (ลูกค้า/สินทรัพย์ที่ไม่เคยถูกจับคู่
เลย — ช่องว่างด้านเนื้อหา), **related** (ความสัมพันธ์หุ้นที่ระบบเรียนเองจาก co-mention)

### 8.8 `/spec` — Spec (เกี่ยวกับระบบ)

หน้าอธิบายกฎทั้งหมดให้คนอ่านเข้าใจได้ — น้ำหนักแต่ละระดับ, สูตรคะแนนเต็ม, รายการ
`SPEC_GAPS` ทั้งหมดพร้อมสมมติฐานที่ระบบใช้แทน, คำค้น macro ที่ทดสอบผ่านแล้ว, บัญชีเนื้อหา
ในขอบเขต

### 8.9 `/settings` — Settings

เหลืองานเดียวคืออัปเดตข้อมูลลูกค้า (ลิงก์ไป `/upload`) กับปุ่ม "จับคู่ใหม่ทั้งหมด" — ปุ่ม
ดึงข่าวถูกย้ายไปอยู่หน้า `/today` แล้วเพราะเป็นสิ่งที่กดทุกเช้า ไม่ใช่การตั้งค่า

### 8.10 `/upload` — Upload (1,059 บรรทัด, หน้าใหญ่สุด)

รับไฟล์คู่ (portfolio + transactions) ตรวจตาม Data Contract ก่อนนำเข้า — **error = บล็อก,
warning = นำเข้าได้แต่ต้องรู้ว่าเสียอะไร** ตรวจ 3 ชั้น: คอลัมน์ที่ต้องมีครบ (พร้อมเลข field
ID เช่น H-01), **คอลัมน์ PII ที่ห้ามมี** (ชื่อลูกค้า, เลขบัตร, เบอร์โทร, ที่อยู่ ฯลฯ — 
`FORBIDDEN_COLS` ใน `upload.py`), และสแกนค่าทุกคอลัมน์หาเลขบัตร 13 หลัก/อีเมล/เบอร์โทรที่
หลงมา หน้านี้ยังมีภาพประกอบ (SVG) อธิบายขั้นตอนการ mask ข้อมูลด้วย Copilot ก่อนอัปโหลด และ
ลิงก์ดาวน์โหลดคู่มือ

---

## 9. วิธีรันระบบ

```powershell
# เปิดระบบแบบคลิกเดียว (production build, พอร์ต 8000)
.\launch.ps1

# โหมด dev (backend :8000 hot-reload + vite dev server :5173)
.\launch.ps1 -Dev

# ตั้งต้นข้อมูลใหม่ทั้งหมดก่อนเปิด (refdata + ลูกค้า + ข่าว + จับคู่)
.\launch.ps1 -Setup
```

CLI งานเบื้องหลัง (ทั้งหมดรันจาก `backend/` ด้วย `python -m scripts.xxx`):

```
python -m scripts.pipeline status              สรุปสถานะทั้งระบบ
python -m scripts.pipeline news --pages 4       ดึงข่าวล่าสุด
python -m scripts.pipeline match                จับคู่ใหม่ทั้งหมด
python -m scripts.pipeline ai --date today --workers 4    ให้ AI อ่านข่าววันนี้ (ล็อตเดียว)
python -m scripts.pipeline daily                ดึงข่าว -> AI อ่าน -> จับคู่ (งานเช้าทั้งชุด)
python -m scripts.pipeline all --pages 4 --ai   ทำทั้งหมดรวม AI

python -m scripts.ai_enrich --date today --limit 5 --dry-run --verbose   ลองอ่านก่อนรันจริง
python -m scripts.ai_enrich --date today --all --workers 4 --redo       อ่านซ้ำทั้งวัน
python -m scripts.check_ai_guards                                       ทดสอบด่านตรวจ AI (ไม่เรียกโมเดลจริง)
```

`--dry-run` สำคัญมากก่อนรันจริงทุกครั้งที่แก้ prompt หรือด่านตรวจ — อ่าน+ตรวจแต่ไม่เขียน
`ai_at` ลงฐาน จึงลองซ้ำได้เรื่อย ๆ โดยไม่กระทบว่าอ่านไปแล้วหรือยัง

---

## 10. งานที่ค้างอยู่ / ควรทำต่อ

1. **Frontend ยังไม่รับ `no_call_th`/`no_call_code`** จาก `/api/articles/{id}/briefing` —
   backend ส่งเหตุผล "ทำไมไม่ตีความ" ไปแล้ว (ข้อ 5.4) แต่ type `Briefing` ใน `lib/api.ts`
   และ component `Briefing.tsx` ยังไม่มีโค้ดแสดงผล ต้องเพิ่ม field ใน type + UI ก่อนที่ RM
   จะเห็นเหตุผลนี้จริง
2. **`AiHealth`/`AiResult` type ใน `lib/api.ts`** ยังไม่ตรงกับ response ใหม่ของ
   `/api/ai/health` (มี `no_call_reasons` เพิ่มมา) และ `/api/ai/enrich` (มี `usage`,
   `flagged`, `demoted` เพิ่มมา) — ใช้งานได้เพราะ TypeScript ไม่ strict runtime แต่ข้อมูล
   ใหม่จะไม่โผล่บนหน้าเว็บจนกว่าจะอัปเดต type + UI
3. **ปุ่ม "ดึงข่าว" บนเว็บยังไม่เรียก AI** (ข้อ 5.7) — มีแผนต่อแล้ว (ข้อ 10.1 ด้านล่าง)
   ยังไม่ได้ลงมือ รอยืนยันตัวเลข `limit`/`workers` ก่อน เพราะเปลี่ยนพฤติกรรมปุ่มจริง
   (เสีย quota อัตโนมัติทุกคลิก + ปุ่มค้างรอนานขึ้น)

### 10.1 แผนต่อ AI enrichment เข้าปุ่ม "ดึงข่าว" (ยังไม่ได้ทำ — รอยืนยันตัวเลข)

**จุดที่ต่อ:** ท้าย `news.ingest_recent()` — วางหลัง `backfill_full_text()` (ต้องมี `full_text`
ก่อน AI ถึงจะอ่านได้) และก่อน `prune_old()` แล้วให้ endpoint `/api/ingest/news` (`main.py`)
เรียก `matching.run_for_articles(only_unmatched=True)` ต่อท้ายเหมือนเดิม ลำดับที่ได้คือ:

```
ดึงข่าว → แท็ก/ให้เกรด (มีอยู่แล้ว) → เนื้อหาเต็ม (มีอยู่แล้ว) → AI อ่าน (ใหม่) → จับคู่ (มีอยู่แล้ว)
```

เป็นลำดับเดียวกับ `scripts/pipeline.py daily` ที่ทดสอบผ่านจริงแล้วคืนนี้ (news → ai → match)
ไม่ใช่ของใหม่ที่ไม่เคยพิสูจน์ — แค่ย้ายมาทำงานอัตโนมัติตอนกดปุ่มบนเว็บแทนที่จะต้องสั่ง CLI แยก

**โค้ดที่ต้องแก้ (สรุปคร่าว ๆ ไว้ก่อนลงมือจริง):**
1. `app/news.py` — `ingest_recent()` เพิ่มเรียก `enrich.enrich_batch()` หลัง `backfill_full_text()`
   จำกัดด้วย `limit`/`workers` คงที่ (ไม่ใช่ปล่อยอ่านทั้งหมดที่ค้าง) แล้วส่งผลกลับใน dict ที่คืน
2. `app/main.py` — `do_ingest_news()` ไม่ต้องแก้อะไรเพิ่ม (จับคู่ต่อท้ายอยู่แล้วถ้า `match=True`)
   แต่ต้องอัปเดต response shape ให้ frontend เห็นว่า AI อ่านไปกี่ชิ้น (ต่อ `lib/api.ts` types ด้วย)
3. `frontend/src/pages/Today.tsx` — ปุ่ม "ดึงข่าว" เดิมต้องโชว์สถานะ "กำลังให้ AI อ่าน…" ระหว่างรอ
   ไม่ใช่แค่ spinner เฉย ๆ เพราะเวลารอจะยาวขึ้นชัดเจน (ดูตัวเลขด้านล่าง)

**ตัวเลขที่ต้องยืนยันก่อนเขียนจริง (กระทบ cost และเวลาต่อคลิกโดยตรง):**

| พารามิเตอร์ | ค่าที่แนะนำ | เหตุผล |
|---|---|---|
| `limit` (จำนวนข่าว/คลิก) | 10 | ตรงกับ `AI_BATCH=8` ที่ปุ่มมืออยู่แล้ววันนี้ ไม่มากเกินจนคลิกเดียวหมด quota |
| `workers` (ขนานกี่เส้น) | 3 | สมดุลระหว่างความเร็วกับภาระเครื่อง/เครือข่าย |

ผลจากค่าที่แนะนำ (อิงตัวเลขที่วัดจริงคืนนี้ — ดูตารางท้ายเอกสารนี้):
- **เวลาที่ปุ่มค้างรอเพิ่มขึ้น**: ~10 ชิ้น ÷ 3 เส้นขนาน ≈ 4 รอบ × ~20 วิ/รอบ ≈ **60-90 วินาที/คลิก**
- **quota ที่เสียเพิ่มต่อคลิก**: ~10 × $0.05-0.08 ≈ **$0.5-0.8/คลิก** (อัตโนมัติทุกครั้งที่กดดึงข่าว
  ไม่ใช่แค่ตอนกดปุ่ม "ให้ AI อ่าน" แยกเหมือนตอนนี้)

**ความเสี่ยงที่ต้องรู้ก่อนตัดสินใจ:** ถ้ามีข่าวค้างไม่ได้อ่านสะสมเยอะ (เช่นไม่ได้เปิดเว็บมาหลายวัน)
คลิกดึงข่าวครั้งแรกจะกิน quota มากกว่าปกติหลายเท่า เพราะ `limit` นับเฉพาะต่อคลิก ไม่ได้จำกัด
ยอดสะสมทั้งหมด — ควรพิจารณาใส่เพดานรวมต่อวัน (เช่น ไม่เกิน 50 ชิ้น/วันไม่ว่าจะกดกี่ครั้ง) ถ้าจะทำจริง
4. **GAP-19/GAP-20** (`tables.py`) — เกณฑ์คะแนน L5 asset class (20) ไม่ถึงเกณฑ์ขั้นต่ำ (50)
   ทำให้ Fund Robo ไม่เคยได้รับ Rebalance/Monthly Report ทั้งที่ R6.16 ตั้งใจให้ได้ ต้องให้
   business ตัดสินใจว่าจะลดเกณฑ์หรือไม่ (ระบุวิธีแก้ไว้ในโค้ดแล้ว ยังไม่ได้ทำ)
5. **`ai_enrich.py --demote-mentions`** ยังเป็น opt-in (ปิดโดยดีฟอลต์) — ของที่กฎจับเกินถูก
   ติดป้ายเตือนไว้ใน evidence แต่ยังไม่ถูกถอนออกจริง ต้องตัดสินใจว่าจะเปิดใช้เมื่อไหร่
6. ยังไม่มี automated test suite ของ backend logic นอกจาก `scripts/check_ai_guards.py`
   (เฉพาะส่วน AI) การเปลี่ยนกฎ matching/mapping/verify ยังต้องตรวจด้วยมือ

---

## 11. คำศัพท์/ตัวย่อที่ใช้บ่อยในโค้ด

- **entity** — "รหัสกลาง" ของสินทรัพย์หนึ่งตัว หุ้นไทย/คริปโต/กองทุนเป็นตัวย่อล้วน
  (`SCB`, `BTC`) หุ้นต่างประเทศเป็น `TICKER:mic` ตัวพิมพ์เล็ก (`NVDA:xnas`, `00700:xhkg`)
- **MIC** — Market Identifier Code (`xnas`=NASDAQ, `xhkg`=HKEX, ...) ดู `tables.py`
- **persona** — กลุ่มลูกค้าตามสิ่งที่ถือเยอะสุด ใช้กรองว่าควรได้ข่าวประเภทไหน
- **STEP1-STEP8** — เอกสาร spec ต้นทาง 8 ชุด แต่ละกฎมีเลขกำกับ (`R1.1`...`R6.16`)
- **GAP-xx** — จุดที่ spec เขียนไม่ครบ/ขัดกับข้อมูลจริง บันทึกไว้ใน `tables.SPEC_GAPS`
- **AI-01 / AI-02** — งานอ่านเนื้อหาเต็มด้วย Claude Code (AI-01 = เติม entity/ทิศทาง,
  AI-02 = ต้องบอกเหตุผลเมื่อไม่ตีความ) ไม่ใช่เลขกฎจาก spec ต้นทาง เป็นเลขที่ตั้งขึ้นเองใน
  โปรเจกต์นี้สำหรับงานส่วนนี้โดยเฉพาะ
