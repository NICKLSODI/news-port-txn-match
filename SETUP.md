# ติดตั้ง MatchPort บนเครื่องใหม่

> **สำหรับ Claude Code:** ไฟล์นี้คือคำสั่งให้คุณทำตามทีละขั้น
> ทำให้ครบทุกขั้นแล้วรายงานผลเป็นภาษาไทยแบบที่คนไม่ถนัดคอมพิวเตอร์อ่านเข้าใจ
> ถ้าติดตรงไหน ให้บอกว่าติดอะไรและต้องทำอะไรต่อ อย่าข้ามเงียบ ๆ

---

## สิ่งที่ต้องมีบนเครื่อง

| อะไร | ใช้ทำอะไร | ไม่มีแล้วเป็นไง |
|---|---|---|
| **Python 3.12+** | ตัวระบบหลัก | เปิดไม่ได้เลย |
| **Node.js 20+** | สร้างหน้าเว็บ | เปิดไม่ได้เลย |
| **Claude Code** | ปุ่ม "ให้ AI อ่านข่าว" | ระบบเปิดได้ปกติ แค่ปุ่มนั้นหาย |
| **Outlook (Classic)** | ปุ่มส่งอีเมลให้ RM | ระบบเปิดได้ปกติ แค่ต้องส่งเมลเอง |

Windows เท่านั้น — ส่งอีเมลใช้ Outlook COM ซึ่งมีเฉพาะบน Windows

---

## ขั้นที่ 1 — ตรวจว่ามีของครบไหม

```powershell
python --version
node --version
npm --version
claude --version
```

ตัวไหนขึ้น error แปลว่ายังไม่มี ให้ติดตั้งก่อน:
- Python — https://www.python.org/downloads/ (ตอนติดตั้ง **ต้องติ๊ก "Add Python to PATH"**)
- Node.js — https://nodejs.org (เลือกตัว LTS)
- Claude Code — https://claude.com/claude-code

---

## ขั้นที่ 2 — ล็อกอิน Claude Code

```powershell
claude auth status
```

ได้ `"loggedIn": true` = ผ่าน ข้ามไปขั้นที่ 3

ได้ `"loggedIn": false` หรือ error ให้รัน:

```powershell
claude auth login
```

เบราว์เซอร์จะเด้งขึ้นมา ล็อกอินให้เสร็จแล้วกลับมาเช็คซ้ำด้วย `claude auth status`

> **เรื่องที่ต้องรู้:** ล็อกอินนี้หมดอายุได้ พอหมดอายุปุ่ม "ให้ AI อ่านข่าว" จะใช้ไม่ได้
> `launch.ps1` ตรวจให้อัตโนมัติทุกครั้งที่เปิดโปรแกรม และถ้าหมดอายุจะเปิดหน้าล็อกอินให้เอง
> ไม่ต้องจำอะไร แค่ทำตามที่หน้าจอบอก

---

## ขั้นที่ 3 — ติดตั้งตัวระบบ

```powershell
cd <โฟลเดอร์ที่วางโปรเจกต์ไว้>

# ตัวหลังบ้าน
pip install -r backend/requirements.txt

# หน้าเว็บ
cd frontend
npm install
npm run build
cd ..
```

---

## ขั้นที่ 4 — เอาข้อมูลลูกค้าเข้า

ไฟล์ลูกค้า **ไม่ได้อยู่ใน git** (เป็นข้อมูลจริง ห้ามขึ้น git) ต้องเอามาวางเอง

วางสองไฟล์นี้ไว้ในโฟลเดอร์ `data/`:
- ไฟล์พอร์ต — ชื่อไฟล์ต้องมีคำว่า `portfolio`
- ไฟล์ธุรกรรม — ชื่อไฟล์ต้องมีคำว่า `txn`

> ถ้าไม่มีไฟล์ ให้ขอจากคนที่ส่งโปรเจกต์นี้มา
> ระบบจะปฏิเสธไฟล์ที่มีชื่อจริง/อีเมล/เบอร์ลูกค้าอยู่ ต้องเป็นไฟล์ที่ปิดบังข้อมูลแล้ว

---

## ขั้นที่ 5 — ตั้งต้นระบบครั้งแรก

```powershell
.\start.ps1 -Setup
```

ขั้นนี้ใช้เวลา **หลายนาที** ทำสี่อย่าง: สร้างตารางอ้างอิง → นำเข้าไฟล์ลูกค้า → ดึงข่าว → จับคู่

รอจนขึ้นว่า `เปิดที่ http://127.0.0.1:8000` แปลว่าเสร็จ

---

## ขั้นที่ 6 — ใช้งานจริง

ครั้งต่อ ๆ ไปเปิดด้วยไฟล์เดียว:

```powershell
.\launch.ps1
```

หรือดับเบิลคลิก `MatchPort.cmd`

ตัวนี้จะ: ตรวจล็อกอิน Claude → เปิดเซิร์ฟเวอร์ → เด้งเบราว์เซอร์ให้เอง

อยากได้ทางลัดบนหน้าจอ: `.\install-shortcut.ps1`

---

## ขั้นที่ 7 — ตั้งค่าส่งอีเมล (ถ้าจะใช้)

ไม่ต้องมีรหัสผ่านอะไรทั้งนั้น ระบบส่งผ่าน Outlook ที่ล็อกอินอยู่บนเครื่อง

1. เปิด Outlook (ตัว Classic) ทิ้งไว้
2. ในเว็บ ไปเมนู **ส่งอีเมลให้ RM**
3. ใส่อีเมลผู้รับในช่อง (พิมพ์แล้วกด Enter ทีละคน)
4. กด **จำรายชื่อนี้ไว้**

ปุ่มส่งจะให้ดูตัวอย่างจริงก่อนเสมอ และมีขั้นยืนยันอีกชั้น — **ส่งแล้วเรียกคืนไม่ได้**

---

## งานประจำวัน (บอกผู้ใช้ให้ทำตามนี้)

1. เปิดโปรแกรม → กด **ดึงข่าว** บนแถบด้านบน
2. กด **ให้ AI อ่านข่าววันนี้** (ช้าราว 15–25 วินาทีต่อข่าว เลขจะเดินให้เห็น)
3. ดูหน้าแรก **งานวันนี้ของ RM**
4. ถ้าจะส่งให้ทีม → เมนู **ส่งอีเมลให้ RM**

งานรายเดือน: กด **ดึงตารางปันผลเดือนนี้** (ปุ่มโผล่เองเมื่อมีรายงานใหม่)

---

## เจอปัญหา

| อาการ | สาเหตุ | แก้ยังไง |
|---|---|---|
| ปุ่ม "ให้ AI อ่านข่าว" หายไป | Claude Code ไม่ได้ล็อกอิน | `claude auth login` แล้วเปิดโปรแกรมใหม่ |
| กดส่งอีเมลแล้วขึ้นว่าเปิด Outlook ไม่ได้ | Outlook ยังไม่เปิด / ยังล็อกอินไม่เสร็จ | เปิด Outlook ทิ้งไว้แล้วกดใหม่ (ระบบลองซ้ำให้ 3 รอบเอง) |
| หน้าจอว่างเปล่า ไม่มีข่าว | วันนี้ยังไม่ได้ดึงข่าว | กดปุ่ม **ดึงข่าว** |
| เปิดแล้วขึ้น error ว่าไม่พบ python | ตอนติดตั้ง Python ไม่ได้ติ๊ก Add to PATH | ติดตั้ง Python ใหม่แล้วติ๊กให้ถูก |
| ภาษาไทยเพี้ยนตอนรันคำสั่ง | หน้าต่างคำสั่งใช้รหัสภาษาเก่า | ใส่ `$env:PYTHONIOENCODING='utf-8'` ก่อนรัน |

---

## สิ่งที่ห้ามทำ

- **ห้าม commit ไฟล์ในโฟลเดอร์ `data/` `logs/` หรือไฟล์ `.db`** — เป็นข้อมูลลูกค้าจริง `.gitignore` กันไว้แล้ว อย่าไปแก้
- **ห้ามเรียก API ส่งอีเมลเพื่อทดสอบ** — มันส่งจริงทันที ไม่มีโหมดซ้อม ถ้าจะทดสอบให้ดูที่หน้าตัวอย่างในเว็บแทน
- **ห้ามแก้ `backend/app/refdata/generated.json` ด้วยมือ** — ไฟล์นี้ถูกสร้างทับทุกครั้งที่รัน `build_refdata`
  ของที่คนตัดสินใจเองอยู่ใน `overrides.json` แก้ผ่าน `scripts/approve.py` เท่านั้น

---

## โครงสร้างคร่าว ๆ

```
backend/app/
  main.py        API ทั้งหมด
  news.py        ดึงข่าว + แกะเนื้อหา + หุ้นเด่น
  matching.py    จับคู่ข่าวกับลูกค้า
  enrich.py      ให้ Claude อ่านข่าวเชิงลึก
  mailer.py      ประกอบ + ส่งอีเมลผ่าน Outlook
  dividends.py   ตารางปันผลจาก INVX Data Book
  yahoo.py       ดึง sector หุ้นต่างประเทศ
frontend/src/    หน้าเว็บ
memie/           ไฟล์ spec ต้นทาง (อย่าลบ — ใช้สร้างตารางอ้างอิง)
data/            ไฟล์ลูกค้า (ไม่อยู่ใน git ต้องเอามาวางเอง)
```

รายละเอียดเชิงลึกอยู่ใน `ENGINEERING-HANDOVER.md`
